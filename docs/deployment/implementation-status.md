# Deployment reset — implementation status

**Active plan: owner's Deployment Implementation Reset Playbook. Old numbered hybrid plan superseded. Deployment readiness:false. Owner-authorized legacy dev teardown complete and independently verified; no new deployment.**

## Checkpoint

2026-09-13: clean `task/#1412-deployment` at `cf65af2c64bd3930908b3890b64636817291fc3c`. Local branch, cached tracking ref and read-only `git ls-remote origin refs/heads/task/#1412-deployment` all match. Earlier history preserved; no reset, force-push, merge or production deployment. Historical checks/commits are in `implementation-history.md`, not fresh validation or an active roadmap.

## C1 — CI repair and legacy source guard

2026-09-15: C1 implementation starts from `eb9fb207672da624765fa854d01ec2bb6f504182`; implementation commit `920ce01fd5eab03710301603b4a9dbf4c5d8dceb`. Cargo jobs bind `COMMIT_SHA` to their actual checkout, Rust matrix jobs pull and inspect the checked-in Postgres reference before tests, and the fixture receives the inspected local ID. `deployment-checks` runs the actual Compose-config test and permits only the cached-helper mount skip. The legacy `deploy.yml` is a manual refusal with no deployment logic or credentials.

Observed GitHub run `35010540269` attempt `1` ([run](https://github.com/aura-historia/backend/actions/runs/35010540269)) used PR head `8ab019614badb25a0602739b3ad33a1190513d1a` and checked out merge commit `ac69718f197450cbd6fa5c9809ab287f776eb3d3`. It completed with **32 success, 2 failure, 40 cancelled, 1 skipped** jobs. Deployment checks failed under Compose `2.38.2`: `198` tests ran, with the one permitted cached-helper skip and one `KeyError: 'env_file'`. Cron library tests passed (`71` passed, `2` ignored), image preparation passed, then reliability tests had `2` passed, `14` guard refusals, and `1` ignored because the intended opt-in was absent while the prepared image override was present. The canceled jobs are not passes.

Local evidence before C1a: Rust1.98.0 formatting, dependency rules, locked workspace Clippy, narrow test-api fixture tests (`29` passed, `3` ignored; fake Docker only), helper/runner regressions (`14` tests), and sanitized deployment checks (`198` tests, exactly one permitted skip) passed. The real GHCR/image-ID preparation and ordinary PostgreSQL jobs were remote evidence, not local execution. `actionlint` was unavailable. No C2, OpenSearch upgrade, live, or deployment operation was performed.

## C1a — CI integration correction

Base head: `8ab019614badb25a0602739b3ad33a1190513d1a`. This local correction installs and verifies official Compose `5.4.0` (`837fd1d35bf6a494f41b5b5988269a7be79de337cf1a1a6ff0e45ab51bb4e9be`) through the same cleared-environment/private-config Docker invocation used by deployment checks. The cron matrix child supplies `AURA_CRON_ISOLATED_LOCAL_POSTGRES=1` and removes `AURA_TEST_POSTGRES_IMAGE`; other crates retain the inspected image ID. Fixture guards and host env-file validation are unchanged.

No corrected GitHub run is claimed yet: this revision is unpushed because no push authorization was supplied. Remote acceptance remains pending an owner-authorized push and exact-run inspection. Local validation and the final local head are recorded in the handoff.

Deployment readiness remains **false**. The branch source guard does not disable workflow copies on `develop`/`prod`, cancel in-flight runs, revoke roles, or change GitHub settings.

Models: orchestrator GPT-6-Astra; delegation exposes no model selector. Requested Terra cannot be selected. Actual agent identities/results recorded below.

## Active sequence

| Milestone | State / executable acceptance |
|---|---|
| R1 | Accepted scope reduction; commit recorded below. Rejected08a removed, nonexistent migrator no longer required; no runtime changes |
| R2 | Accepted locally: four images built and actually run; independent repeat smoke passed. Idle startup only, not deployment readiness |
| R3 | Accepted isolated empty-stack startup/same-version restart; all13 apps, real Sequin/Caddy, platform state retained. Live-dev readiness still blocked |
| R4 | Accepted isolated idle A→B/bad-candidate host-command capability; active custody, loaded-traffic drain, backward redeploy, reboot and live gates remain |
| R5 | Offline DB-only NAT/CA integration implemented and tested; live EIP/firewall/TLS/business-invocation acceptance blocked pending explicit target/inputs/approval |
| R6 | Not started: environment-approved develop workflow around proven host command |
| R7 | Not started: CalVer promotion and previous immutable release redeploy, rehearsed outside prod |
| R8 | Not started: prove native dev API before CloudFront/Caddy public-origin cutover |

Migration scope is resolved: **fresh initialization only**; no adoption, historical backfills, incremental framework or schema downgrade. Do not ask again. Dormant legacy manifest/protocol metadata is not used for current development deployments. Missing fictional migrator no longer belongs in the actual component catalog.

## R1 — scope reduction

Remove only `deploy/control/src/host/application-compose.ts`, its dedicated test and `deploy/compose/README.md`. Existing runtime, TLS/custody fixes, fresh bootstrap, generic helpers and NAT source/tests stay unchanged. No replacement renderer or owner-aware reconciler. Current catalog is four native/five Lambda/ten worker scopes; dormant parser permits the four real images without requiring historical synthetic migrator identity, while missing real applications still reject.

Archive previous status/decisions with explicit superseded headings. Active decisions and nearest AGENTS now require ordinary Compose/restart policy/flock, direct runnable acceptance and correctness+simplicity review. Fresh Node26.8.2 TypeScript build/schema generation/**594 tests**/catalog pass. Only generated schema diff: native minItems5→4. Deleted ignored prior dist output before rebuild; no stale renderer test credited. Independent reviewer `655af283-b9b6-44bb-a079-8ce2cbbbb9e3` (GPT-6-Astra) accepted correctness and simplicity, independently183 catalog/release tests plus current-source/24-output byte match and each-missing-real-image rejection matrix. Cargo/Rust/migrations/infra/workflows byte-identical to checkpoint; no runtime regression from changed call sites. Whitespace pass. Accepted commit: `83e41ab2f5fcd1c5fd31f41a1be3cede149c43da` (`revert(deploy): remove rejected renderer and activate simple deployment reset`). Final183 targeted tests pass after stale migrator-comment correction. No replacement abstraction.

## R2 — runnable images accepted locally

Source/build baseline: `672bdcefdeaabc6cd9f78461ec1bf859c31dc443`, `task/#1412-deployment`. Rust/Cargo/toolchain/migration/search inputs unchanged. New recipe/tests were uncommitted during execution; image labels identify source, not cryptographic build provenance. Accepted capability commit: `b1e743ae740b9512019d95d3832387b79a77f976` (`feat(deploy): build and smoke-test runnable native OCI images`). Final staged whitespace check and six Python tests passed; nothing pushed or deployed.

`deploy/images/Dockerfile` builds four actual nonroot linux/amd64 executables with pinned Rust1.98.0/base digests/signed Debian snapshot and locked Cargo. Fixture-only helper reuses existing `bootstrap-local`; neither bootstrap nor Python ships in application targets. `deploy/tests/README.md` records all immutable image IDs and exact reproduction command; `deploy/images/README.md` records builds and runtime inputs/budgets.

- Earlier230s cold build timed out, exported nothing; not credited. Owner approved30min retry: shared release compilation **14m56s passed**, remaining three targets reused it. Helper build **5m42s passed**.
- Integrator actual smoke **PASS**; independent reviewer repeated exact immutable-ID command: **PASS**. Genuine separate fresh SQLx histories, restricted runtime role, real OpenSearch3.1.0 mappings/file-backed analysis. ADC/JWKS/SQS are explicit non-forwarding doubles in a loopback-only Docker namespace, not cloud integration.
- Four binaries/two worker scopes: ten READY/version/nonroot/library/SIGTERM/SIGINT launches, five read-only preflights, startup negatives, empty worker polls/normalizer turn. Owned cleanup confirmed; independent before/after container listings empty. No unowned cleanup/prune.
- Six Python regressions pass. Earlier mapping-readback assertions failed because OpenSearch omits redundant object/default enabled:true fields; test-only normalization narrowly handles those omissions. Production definitions unchanged. SHA guard/nested-target exclusion and ownership/fault-witness checks repaired before acceptance.

Implementer `8d339547-8a9a-464d-844a-261d3ecc03b9`; smoke draft/investigation `e5114dd9-f895-40b3-959a-e33bae90798b` (no startup credit); smoke repair `23275748-2516-4c0f-95a0-13eb5c6697ca`. Source reviewer `78c77645-459c-4b1b-84fb-1f8ec0d7b9e0` independently38 SHA cases; smoke reviewer `97da37d2-49bd-49dd-9a4d-e7719ad30b8a` cleared repaired attempt. Final independent reviewer `88995a93-b9c4-40bf-8080-f5989ff6890f` **accepted correctness and simplicity**, reran six tests/full smoke, checked unchanged source. All GPT-6-Astra. Its nonblocking source-identity wording finding is corrected. Harness stays test-only; no replacement deployment abstraction.

Limits: idle startup only. No all-ten-scope, real provider/TLS/JWT authentication, SQS/Sequin custody, active-work drain, full Compose, A→B, reboot or live deployment acceptance. Canary checks cover probes/retained1MiB logs only. Runtime/infra/workflow code unchanged; no architecture deviation. Removing this slice does not alter existing runtimes or schemas; cached images retained, disposable fixture state removed.

## R3 — ordinary Compose, isolated acceptance

Baseline `5b8914085c0819c0d8cbdfbbe344128ff8919e04`, clean `task/#1412-deployment` before work. Accepted capability commit `588b3f3cf140d831a35e16b5b1c1493218e5751a` (`feat(deploy): run isolated full backend with ordinary Compose`). Final43 Python tests and staged whitespace check pass. Nothing pushed or deployed. Runtime/Cargo/schema/search/infra/workflow sources unchanged; reused R2 binaries retain source identity `672bdcefdeaabc6cd9f78461ec1bf859c31dc443`, not the tooling SHA.

`deploy/compose/` now has ordinary separate platform/application/edge projects and nonsecret input example/runbook. Long-lived services use unless-stopped; applications are nonroot/read-only, default stop budgets60/300/330/330s. Platform volumes remain independent. Only Caddy publishes loopback HTTPS and joins an additional edge bridge; app/platform test network is internal. No renderer, secret materializer or new deployment framework.

Actual test: `PYTHONDONTWRITEBYTECODE=1 python3 deploy/tests/smoke-compose.py` **PASS**, then independent post-repair repeat **PASS** within30min. Same three Compose files plus explicit test-only override, genuine fresh SQLx histories/TTL3/OpenSearch, three separate databases, Redis, Sequin. All13 apps ready/identity-matched; ten scopes complete source/DLQ checks and empty polls. Ten actual Sequin worker-DNS bindings, restricted-role active replication, pause_on_full and zero backfills verified. Caddy trusted local-CA/hostname HTTPS succeeds, untrusted CA rejects, safe GET matches API404. App-only stop/start exits0/removes runtime PG sessions, preserves platform/edge container IDs/start times/volume records, then all13 return ready; checked stores/histories remain empty/unchanged. Exact owned containers/volumes/networks/temp directories removed and independently checked absent. Test implementer reports one earlier exact-manifest Caddy pull. Independent review runs performed no builds or pulls.

Failures repaired before acceptance: quoted tmpfs commas (base YAML); internal-only bridge could not publish Caddy port (Caddy-only edge bridge); invalid test ProductListingId; empty Compose IPAM normalization; draft Sequin endpoints replaced with real worker endpoints before final acceptance. Independent reviewer held volume/bind isolation gates; fixed exact project-owned local-volume and per-service fixture-bind allowlists, actual mount/env checks, backend network drift checks and revalidation before mutation/cleanup. No weakened runtime/security gates. **43 Python tests pass**, including negative ownership/storage/mount/network/provider cases. Reproduction/pins and operator inputs: `deploy/compose/README.md`.

Investigation: `f3d64531-edaa-491d-8040-381d61c064b8` runtime; `55a8122e-dfac-497e-a3f6-65afe5b1ded4` platform. Provider implementer `94ea3bf5-1b95-4665-9980-bbb17187f611`; Compose test implementer `11034af4-0e2b-4ae8-9ece-505577a2e1ee`; integrator owns ordinary Compose/docs. Independent reviewer `94551bf9-97f7-491c-a977-1881984363b7` accepted **correctness and simplicity**, independently43 tests and full post-repair smoke, zero owned resources remaining. All GPT-6-Astra; no selectable Terra. Nonblocking Zoho-input count corrected.

Acceptance limits: empty startup and same-version application restart only. SQS/ADC/JWKS doubles, not real provider/auth or active custody. No queue sends/purges, active CDC handling, A→B/bad-B, engine upgrade/reboot/TTL expiry, production acceptance or whole-stack no-egress proof. Sequin0.14.6 certificate verification, real-stage fresh initialization, OpenSearch trust/security bootstrap, approved provider credentials/queues/assets and host inputs remain real-deployment gates. Existing local-only bootstrap is not relabeled for real use. No general migration/backfill machinery added. Removal affects only these Compose/test/docs files; no existing runtime/schema changes, fixture resources removed, cached images retained.

## R4 — thin host command, isolated cutover acceptance

Baseline `08173849aa0a5f5849a340a1030b94af2162aee8`; branch clean before work. Accepted capability commit `49c833c7b816506d7fb7e12905b9c1555d302f2b` (`feat(deploy): replace running applications with guarded API cutover`). Final70 Python tests, CLI help and staged whitespace checks pass; no owned rehearsal resources remain. No Rust/Cargo/schema/infra/workflow changes. `deploy/bin/deploy` uses Python stdlib/flock, preloaded immutable IDs and current/previous/incomplete files. Two fixed API slots via an ordinary Compose overlay, private Caddy validate/reload, exact-ID stop/confirmed removal before replacement, preserved platform/Sequin state. No automatic rollback, secret renderer, migration or ownership framework. Host configuration/manual changes must cooperate with the same flock; recovery only verifies already-converged original/target after explicit prior-command completion confirmation.

A source remains `672bdcefdeaabc6cd9f78461ec1bf859c31dc443`. Builder `8f1ead3a-fa3e-4571-9b52-f274f351f276` actually compiled all four B binaries from `08173849aa0a5f5849a340a1030b94af2162aee8` in **15m41s** under30min; final targets loaded successfully. Byte/mode comparison of1304 build-input files unchanged before/after. A/B Rust sources are unchanged; this proves different built release identities, not changed business behavior. B IDs, reproduction and operator recovery: `deploy/bin/README.md`. Test fault API wrapper is explicitly B-derived exit42 injection, not a source-built release.

Actual `PYTHONDONTWRITEBYTECODE=1 python3 deploy/tests/smoke-deploy.py` **PASS**; independent final post-fix repeat **PASS** under30min. Host CLI adopts verified A, runs B preflight/candidate, replaces all12 workers/jobs, switches Caddy then removes A API. Docker events prove old death/removal before successor creation; all13 B processes pass image/source/readiness checks. Trusted HTTPS/disk/container/active-config and unique Docker DNS upstream identity agree. Current B/previous A recorded. Bad candidate leaves B untouched; incomplete blocks apply; recovery rejects missing confirmation and mixed state, then preserves previous A after explicit cleanup/convergence. Real-process flock contention rejects. Platform/edge IDs, start times, volumes and Sequin configuration/retry settings remain unchanged. Final owned projects/volumes/networks/temp directory independently absent; cached images retained.

Pre-acceptance failures fixed: Compose duration normalization; Caddy warning stderr separation; nonsecret next-config0644 versus state0600; protected test installation copies instead of chmodding user checkout. A proxy-validation failure retained mixed A-API/B-workers state; exact owned fixture teardown was explicitly inspected/approved, **not rollback/convergence**. Reviewer findings fixed: input-drift rejection before mutation, actual process/mount checks, Caddy image/mount/config identity, Docker Aliases+DNSNames+default-name exclusivity, safe typed status output, and previous-reference preservation across interrupted file writes. No test/security gate weakened.

**70 Python tests pass**, including corruption/redaction, DNS/mount/input drift, unknown command/stop refusal, state-write interruption and recovery negatives. Investigator `85b58e77-7c09-417b-a6d9-ec489e568246`; tester `420779cc-ec77-4d82-9cd8-fcea22a2f995`; integrator owns command/Compose/docs. Independent reviewer `0bf1edd4-c8a8-4183-a8da-670f111e73f3` accepted correctness **and simplicity**, independently70 tests plus full final rehearsal and cleanup. All GPT-6-Astra; requested model selector unavailable. No pulls/Cargo builds/cloud calls during independent rehearsals; only local fault-wrapper image build.

Limits: idle API/application replacement, not real SQS receipt/redelivery custody or active CDC handling. No loaded-traffic continuity/active-work deadline proof, backward-release rehearsal, host/engine reboot, real-provider/TLS bootstrap or live-dev/production approval. Localhost-only Caddy; R3 real-stage gates still apply. Nothing pushed or deployed. Code removal requires explicit service/config convergence first if the host command has been used; never remove state/lock files or recreate a retired slot blindly.

## R5 — opt-in DB Lambda egress and actual CA layer

Baseline `da8a969e1a8af6156598457b38897896734706d7` on `task/#1412-deployment`; only the prior agent's two untracked CA files at resume. Accepted offline capability commit `273ac8ddb06956e294535913e151d827da95c10e` (`feat(infra): attach DB Lambdas to fixed NAT egress and CA layer`). Final staged whitespace check passed. Nothing pushed or deployed. No live mutation. `infra/bin/app.ts` accepts explicit local `postgresLambdaConfig`; absent input preserves all legacy templates and ephemeral rejects opt-in. Data instantiates existing NAT/EIP construct; compute attaches only four catalog DB Lambdas to private subnets/sole SG, actual public CA layer and per-function reservations. Shopify polling cap matches reservation. Exact source-function-ARN deny confines ENI actions to Lambda service, not application code. IDs/queues/invocation targets stay stable. Explicit enabled FX schedule dependency, no new framework or migration work.

Pinned Node26.8.2 `npm --prefix infra run build` and full fresh Jest **489 tests/7 suites pass**. Six actual installed-CDK CLI synth cases pass: default dev/prod/ephemeral, single-stack ephemeral, opt-in dev/prod. Credential/config files and metadata disabled with `env -i`; no `npx`, AWS/provider calls, installs or asset uploads. Exact public CA bytes and actual staging bucket/stage-prefix wiring checked. All owned CLI inputs/output/logs removed. OS denied `unshare -n`; no bypass or claim of network-namespace isolation. Existing ts-jest/CDK/Node20-provider/AZ warnings remain; no gates weakened.

CA implementer `7a41cfc6-6979-450e-9927-6c387602c9f4`; config implementer `4d7ceae8-434c-4b5d-909b-0c30cb9a0f15`; integration tests/CLI synth `f0dc28f1-1936-4edc-8001-e9102e97ceeb`. Integrator owns shared wiring/docs. Independent reviewer `70074b4e-ebbb-4f3e-baa9-6840a1238a8d` accepted offline correctness **and simplicity**, independently489 tests, acyclic IAM/stack dependencies and all11 default templates identical to baseline. CA staging umask finding fixed; tests now reject group/world-writable staged directories. Config-test mock typing fixed. Nonblocking FX/Shopify coupling replaced by explicit internal ordering flag. All reported GPT-6-Astra; no selectable Terra. Final post-doc review required an invocation hold that survives CloudFormation update/rollback; runbook now warns managed enabled states and fresh sources can deliver before post-checks. Template-equality wording corrected to structural equality. Test subprocess PATH uses the running Node, not an operator-specific install. Final reviewer sign-off accepted code/runbook with no outstanding offline blockers; integrator then reran build and all489 tests successfully.

Runbook: `infra/postgres-lambda-r5.md`. Deploy data only first; obtain actual EIPs and separately approve firewall/TLS/schema readiness before compute. Initial FX resource Create invokes real provider/business writes; Update/Delete do not. Dependencies neither validate external readiness nor pause existing triggers. Reservations×pool only estimates active-invocation sessions; warm/frozen/replaced instances can retain more. Existing ordinary SSM references neither create separated SQL roles nor establish approved secret storage. Do not omit config as application rollback or roll CA/credentials/schema back with `CommitSHA`.

Limits: no AWS resource deployment/inventory, CA upload/mounted-file proof, real Lambda/Postgres TLS, source-EIP/firewall exposure test, trigger pause/resume, live credential review or rollback rehearsal. R5 live acceptance and R6–R8 stay blocked; workflows unchanged. Handoff records one prior read-only STS identity query: account `550663032924`, root principal. It is not an approved dev target; credentials unused in this resumed implementation. No host/CA/region/firewall authority inferred.

## Owner target and read-only discovery

Baseline `a2e98e07d802a8796157ac5bf2b74bf49be34912`, clean before discovery. Owner selects **this machine** for the complete self-hosted dev stage, AWS dev separately; prefer `eu-central-1` for regional resources. Initial inventory alone authorized no host/cloud/GitHub mutation. Legacy retirement was separately authorized afterward and is recorded below; no new runtime deployment or workflow changes.

- AWS: initial STS failed with expired session. After owner `aws login`, STS confirms account `550663032924`, root principal. Regional read-only discovery used eu-central-1; IAM is global. No narrowly usable operator/runtime role found: `github-ci-deploy` is AdministratorAccess with `repo:aura-historia/backend:*` OIDC trust; regional Roles Anywhere anchors/profiles absent. Do not reuse CI/root for new host runtime or routine deployment.
- Host: Ubuntu24.04.4/x86_64, 6cores/12threads,62.7GiB RAM (25.7GiB available at sample),~751GiB disk available, no swap. Docker29.7.2 rootful/Compose5.4.0; no containers, default networks only. Required config/state paths absent; current user UID1001, Docker socket access works, `sudo -n` requires password. Container limits total20.75GiB steady/21.75GiB candidate: modest memory headroom, not measured workload acceptance.
- Host interfaces: `eno1` has `148.251.91.20/32` and `2a01:4f8:202:7118::2/64`; `docker0` uses172.17.0.0/16. This proves configured addresses, not static assignment or external reachability/firewall safety. TCP443 bound on all IPv4;9200/9300 on loopback. `systemctl show` confirms active nginx/opensearch; socket ownership was not visible without privilege. Owner subsequently confirms nginx/opensearch are obsolete deployments and authorizes stopping/removing those services. Initial noninteractive sudo stop failed before execution. Owner then ran privileged retirement; verified both units inactive/dead/disabled and no TCP443/9200/9300/5432/8443 listeners. Data deletion was not performed. Memory available after retirement:36.6GiB (snapshot, not workload acceptance). Preserve on-disk data and unrelated services; free ports are not reservations.
- GitHub: repository `aura-historia/backend`, default `develop`, viewerPermission ADMIN. Metadata GETs work; write/approval permissions not tested. `aws-dev` requires `bruderj15`, allows self-review/admin bypass, branch policy develop only. No `aws-prod` returned. No tag-targeting ruleset returned; legacy tag endpoint404 is inconclusive. Existing deploy workflow remains branch/manual-triggered; only CDK job environment-gated, push/manual concurrency differs, CI is not its prerequisite. No settings changed or workflow dispatched.

Investigators `d7f5d03d-48b2-48e5-b71f-3620284ccb26` (host) and `24da89a3-c074-4900-bf46-ede9b3f8729a` (GitHub), both GPT-6-Astra; no model selector. Findings are snapshots, not service/data ownership, real-stage bootstrap, public-exposure or production acceptance. No new test run needed for this documentation-only discovery; previous R5 validation remains historical.

## Authorized legacy AWS dev teardown

Owner explicitly permits removing obsolete AWS dev, including old state; local-service permission was not silently extended. Discovery found three legacy application stacks, 31 regional Lambdas,13 enabled SQS mappings,15 enabled stack rules and a running DynamoDB pipe. Current ten native source/DLQ pairs, NAT/EIPs and native ECR repos absent. Existing VPC CIDRs172.31.0.0/16 and10.0.0.0/16 must not be reused blindly. CloudFormation status is not runtime health or formal drift proof.

Operation `legacy-dev-teardown-20260914`: **complete — independently verified**, exact account550663032924/eu-central-1 stack IDs, sequential API → compute → data:

- API: `arn:aws:cloudformation:eu-central-1:550663032924:stack/application-dev-api/e6c6dc80-7b6d-11f1-88cb-0a33dd71aac7` — first attempt token `legacy-dev-teardown-20260914-api` ended DELETE_FAILED. CloudFront E1KJ7TJ0K7V353 was disabled/Deployed; deletion rejected HTTP412/InvalidRequest because of pricing-plan subscription. API Gateway stage also failed mapping dependency; dev-domain mappings were empty, stage dev remained. Many API child resources already deleted: not rollback. Owner reported plan cancellation. At resume HEAD457b809a0, root/account reverified and no active deployment run. Normal same-stack retry `legacy-dev-teardown-20260914-api-retry1` accepted; final DELETE_FAILED. Retry events confirm CloudFront distribution, WAF/provider and dev custom domain DELETE_COMPLETE; pricing no longer blocks resource deletion, billing settlement unverified. At that point, only API/stage remained. All-domain mapping inspection found `api.aura-historia.com` root mapping `o7vx2a` → `exdc0l6vu6/dev`; this external production-facing route blocked stage deletion. Owner explicitly authorized removing only root mapping `o7vx2a`, accepting routing interruption while preserving custom domain/DNS/certificate. Resume HEADa800f2f60: root/account, exact mapping and no active deploy run reverified. Mapping deletion succeeded; get-api-mappings returned empty and custom domain remained AVAILABLE. Normal API retry2 token `legacy-dev-teardown-20260914-api-retry2` accepted and verified DELETE_COMPLETE. No DNS/certificate mutation or force-delete. Compute/data completion recorded below.
- Compute: `arn:aws:cloudformation:eu-central-1:550663032924:stack/application-dev-compute/9f4f6e50-7b02-11f1-a8f6-0246fe38417f` — API completion confirmed, UPDATE_COMPLETE/unprotected, both exports report no importers. Delete request accepted with token `legacy-dev-teardown-20260914-compute`; verified DELETE_COMPLETE.
- Data: `arn:aws:cloudformation:eu-central-1:550663032924:stack/application-dev-data/6dc082c0-7b02-11f1-a268-0ae94f9568fb` — compute completion confirmed, UPDATE_COMPLETE/unprotected, all five exports have no importers. Delete request accepted with token `legacy-dev-teardown-20260914-data`; verified DELETE_COMPLETE.

Preflight: all three UPDATE_COMPLETE/unprotected, no execution RoleARN, no retention/snapshot policy. Final verification after the authorized mapping removal: all three DELETE_COMPLETE,359/359 resource summaries complete, none skipped or failed. Template ownership excludes shared S3/OIDC/GitHub/DNS/QuickSetup/prod resources. Authorized teardown completed, including old Cognito users/client/domain, table_1-dev and26queues/messages. Shared resources remain untouched; external DNS can become stale. No active deploy workflow at preflight; workflow remains enabled, so this is not a permanent concurrency fence.

Independent reviewer `2a9ff24e-a5a6-4cac-809e-bdf2be29fe12` (GPT-6-Astra) approved exact-stack ordinary CF deletion; import checks confirm API imports compute and compute imports data only. WAF provider live code/hash inspected: exact dev ACL name/physical ID, sole attachment to owned distribution, reverse dependency correct. Global exception is CloudFront WAF in us-east-1 (`application-dev-api-cloudfront-web-acl`, ID8ee7c421-abd8-4fc4-a8f5-f103fbff5779); no other deletion logic. Root credential use is confined to this explicitly authorized legacy teardown; no new role/trust configuration. CloudFormation removed only the stack-owned legacy IAM resources. On failure/unknown outcome, inspect exact stack/events; no force-delete or automatic wider cleanup. Reviewer independently diagnosed both failures; subscription/billing changes are a new unapproved operation, not silently included in CF retry. Installed AWS CLI2.34.60 has no CloudFront pricing-plan operations/fields or installed PricingPlanManager service; actual plan ID/tier/price/effective date remain unknown. Public AWS docs describe PricingPlanManager, but no alternate client or undocumented endpoint was used. Owner subsequently reported cancelling the plan; successful distribution removal verifies deletion is unblocked, not plan tier or billing settlement. Retry1 stage failure independently diagnosed using only matching request-token events. Same reviewer first approved stopping at the external production-hostname mapping boundary; owner then separately authorized that exact mapping removal, after which normal deletion completed. Do not confuse monitoring subscription with pricing.

Read-only investigators: resources `b0abd22d-dd7a-4cd1-9d62-baa3a36d64da`, IAM `163bbe93-ef89-4eb9-9b3a-4f9ef1d913ff`, both GPT-6-Astra. Final independent verifier `2a9ff24e-a5a6-4cac-809e-bdf2be29fe12` confirmed exact stacks/359children complete, all31 old Lambda IDs/13mapping IDs/26queue IDs absent, and exact table/pool/API/VPC/CloudFront/WAF absence via metadata reads. Preserved `api.aura-historia.com` AVAILABLE with no mappings; referenced ACM certificate ISSUED. Domain/certificate presence is not a byte-preservation or DNS-diff claim. Shared buckets/OIDC/GitHub/DNS/QuickSetup/unrelated prod untouched by this cleanup; no exhaustive orphan/log/artifact scan. No billing-settlement or new-deployment acceptance. No new application code or tests; documentation whitespace validation only.

## Sequin verified transport — isolated acceptance

2026-09-14 resume baseline `8ac6f424f87de45e28fdeed657159cfd28f11e81`, branch `task/#1412-deployment`; slice uncommitted at testing. Accepted capability commit: `7ffc8dc7e3848719d815d7bdd795c259abdcc169` (`feat(deploy): verify Sequin PostgreSQL transport with stock stunnel`). Nothing pushed or live-activated. New ordinary `compose.sequin-tls.yml` runs stock nonroot stunnel5.80 beside unmodified Sequin0.14.6. PostgreSQL SSLRequest negotiation, CA-chain/hostname verification and TLS≥1.2 protect metadata/source transport. Plaintext is confined to shared loopback—not Sequin-native end-to-end TLS. Metadata wrapper unsets truthy-empty `PG_URL`; every source must explicitly route through loopback15433. No Rust/schema/infra/workflow changes or new deployment framework.

Built sidecar37.1s; immutable local ID `sha256:e740ea04ebd5d3d70e6ac8822d390f8a3ceea90ae3465fc930644689ebc479ee`. **Full integrator probe PASS; independent exact-command repeat PASS; 24 guard tests PASS.** Actual metadata trust negatives block migration; valid metadata/SQL/WAL sessions and committed delivery; private listener isolation; source CA/hostname/server-no-TLS negatives with independently attributed SQL/WAL failures, no source authentication/delivery, healthy metadata, same-slot/sink recovery and no backfill. Reproduction/pins/limits: `deploy/tests/README.md`; host configuration: `deploy/compose/README.md`; image: `deploy/images/README.md`.

Failures fixed before acceptance: Docker local logging rejects single-file compression (test-only compression disabled); draft failure signatures did not match real DBConnection/SlotProducer/stunnel output. SQL now joins fresh failed-connect PIDs to read-only runtime state; WAL requires both source/replication UUIDs. Exact transport-close boundaries reject lookalikes. Stunnel `protocol.c:813–822` at upstream `62ab5ccf33485b81bc4eaf0c76fd5558d2458e08` rejects SSLRequest refusal. Source tracing pins Sequin `0aae94418e4d80d269baba26c840db8ecfc0580c`; actual release reports Postgrex0.19.3/DBConnection2.7.0. No timeout/absent-delivery substitution for trust evidence; no raw logs printed.

Interruption handling: prior delegation-limit error had left three never-started containers; resumed inspection reconciled only that exact ownership scope before removal. First direct retry likewise retained three Created/PID0 containers on logger failure; explicitly inspected and removed without force. Later failed assertions and both final successful probes confirmed owned cleanup. No global prune or live-state deletion. Temporary diagnosis inputs are not runtime configuration.

Implementer from handoff `e45b8a58-8480-4748-83b7-e9da011bcc43`; prior pre-run reviewer `4b15dcde-d9f2-41b8-a135-ef838bc2314b`. Current investigator `c6c33532-4554-47a2-ad63-7f43d92b292f`; independent final reviewer `f90e787f-d505-4d34-9dbd-867b73453e63` **accepted correctness and simplicity**, ran24 units/full probe once and verified zero owned containers/networks/volumes/processes/temp directory afterward. Available delegation resumed; actual model GPT-6-Astra, no selectable Terra.

Limits: synthetic SQL/WAL only, no real worker custody/load, expired-certificate test, live trust/rotation, namespace replacement or reboot proof. Real host CA/endpoints/source routes and coordinated two-service platform setup remain required. Removing the overlay after activation would remove verification; never fall back to native `verify_none`. No AWS/GitHub/DNS/firewall or live-dev mutation; no new deployment acceptance.

Final integration: **94 deployment Python tests pass**, whitespace check passes. Reviewer confirmed corrected CA filename and pre/post-start inspection wording; docs accepted. Both retained never-started fixture directories were removed only after exact-owner/resource-absence/no-mounted-input checks. Build scratch/IID removed; cached images retained. No TLS probe resources remain.

## Explicit dev fresh initialization — isolated acceptance

Resume baseline `13a4b822673e0a33ed0e90ddee4f90889a097d4a`, clean `task/#1412-deployment`. Accepted capability commit `ff146360c6a82cc0e2db06fcc396b020386bfcea` (`feat(deploy): initialize fresh dev databases with verified TLS`). Staged whitespace passed; build IID scratch removed, cached immutable image retained. No push/live activation. New `crawler/bootstrap-dev` uses the existing private fresh initializer/verifier with fixed dev-only/shared verify-full policy; local command safeguards unchanged. Separate nonroot image and ordinary help-default/no-restart one-shot Compose, separate protected initialization inputs. No new migrations, catalog prerequisite, adoption, upgrade, backfill, repair or orchestration framework.

- Image build **1265.4s PASS**; local ID `sha256:d5fcd8e90ce6b1e414559bcd0f8eca3dbb15796fdf35ee30d8a01054b25f7de7`. Source/recipe uncommitted during build; baseline label is not clean-commit provenance. Four daemon images/entrypoints stay separate. Reproduction: `deploy/images/README.md`.
- **Integrator full real Compose probe PASS; independent exact-command repeat PASS.** Both fresh SQLx histories match source; repeat initialization refuses; selected-target TLS, wrong CA/hostname/password and plaintext refusal with positive controls/attributed evidence; restricted read-only verification. Logical snapshots/history/globals/TTL configuration unchanged on rejection/verification and in unselected DBs. Nonroot/no Docker/socket. Both runs confirmed owned cleanup; independent final labels show zero containers/networks and both directories absent.
- `cargo test -p crawler --bin bootstrap-dev --bin bootstrap-local --all-features --offline --locked`: **240 tests per binary PASS**. `cargo fmt --all -- --check` PASS. **109 deployment Python tests PASS**, independently repeated. `npm --prefix deploy/control run validate-catalog` PASS (4 native/5 Lambda/10 scopes/25 mail, no incomplete). Initial catalog run refused the new auto-discovered bin; added only `bootstrap-dev` to excluded operator tools alongside `bootstrap-local`, never a mandatory application artifact. No broader Rust/live/cloud checks claimed.
- Existing library source-layout assertion `should_use_strict_environment_adapter_at_every_postgres_entrypoint` **fails** at `src/crawler/src/local_db/config_tests.rs:428`: thin server/bootstrap wrappers lack literal parser text. Reviewer byte-compared test/wrappers to baseline and confirmed inherited failure. Left unchanged; earlier incorrectly exact-qualified invocation selected zero tests and is not validation credit.

Independent review caught generic TLS-failure acceptance and lost history-mismatch evidence; repaired with case/PID/reason attribution, positive controls and one central retain-on-failure boundary. Removed duplicated Compose YAML in favor of frozen hash. Four early real attempts failed on narrow Compose/Docker representations or post-exit peer discovery. Each retained synthetic fixture was explicitly ownership/command/mount/network checked before exact removal; no blind retry/prune. Final fixed test peers derive from the owned subnet, no application topology change. PostgreSQL plaintext HBA is test-only fallback detection.

Rust implementer `e0c8891c-c4de-4115-b5b5-79fc04bfd153`; test implementer `68d346b3-cca7-47b2-aed3-9b42f4769c5d`; integrator owns packaging/Compose/docs. Independent reviewer `e7b47881-0ed2-4e62-b030-56209f8ffc24` accepted **correctness and simplicity**, ran full final probe/all109 Python tests and checked owned cleanup. Final docs/catalog signoff accepted after command chaining prevents second-target initialization following first-target failure and verification wording distinguishes migration locks from ordinary read locks. All available GPT-6-Astra; no Terra selector. Read-only search investigator `9ad4c9b1-15a5-457a-bdc4-dc2655a1cd10` identified next concrete gap: security-enabled OpenSearch bootstrap and explicit CA wiring for API/worker/cron SDK clients plus worker preflight; no implementation or live acceptance claimed.

Limits: synthetic superuser initialization, not real dev grants, full DDL drift, TTL-worker health/restart, crash/commit-loss coverage, cross-target atomicity or live authority. Real host DB/role/CA/firewall/provider inputs remain required. Runtime grants are a separate gate. Host operator commands and failure handling: `deploy/compose/README.md`; exact test/evidence limits: `deploy/tests/README.md`. Removing this capability never undoes committed SQL; after use inspect effects, do not downgrade/reset. No AWS/GitHub/DNS/firewall or live-dev mutation.

## Secured OpenSearch fresh setup — isolated acceptance

Resume baseline `02e842f23aaa5ccb54c225d6b1307cbc1df5f175`, `task/#1412-deployment`. Accepted capability commit `b9d1fa71d90f06febae2a336eebf2500c4a7ce6d` (`feat(deploy): initialize and verify secured fresh OpenSearch`). Code and documentation independently accepted; staged whitespace passed. Git reported existing loose-object/gc warnings; no pruning or repository maintenance performed. Stock-native security setup and fixed `deploy/bin/opensearch initialize-fresh|verify` are exercised, not a planner/framework. Separate UID1000 stock admin tool/inputs, ordinary one-shot Compose, current mappings/analysis and new current RRF asset. Exact cluster/version/verified TLS, admin-cert GET-only verification, fresh-only create, no repair/retry/adoption. No Rust/Cargo/infra/workflow/main-platform-Compose changes. No push/live activation.

**Full tester rehearsal PASS; independent exact-command repeat PASS; all180 deployment Python tests PASS.** Five role grants, both own-index projectors, standard and exact item-level bulk denials; fresh/missing/partial/repeated/populated checks with unchanged complete snapshot sections; content-free external-version fences beyond delete GC and after node restart; synthetic768-vector hybrid and multipage PIT/percolation; wrong CA/hostname/password/plaintext and node/unregistered admin-cert refusals. Checked data/security/settings survive same-container restart. No system-alias filtering; logical hashes are not whole-plugin/physical-state attestation. Both successful runs confirmed exact-owned cleanup; independent process/label/temp checks found no residue.

Earlier failed runs remain failed. Fixed stock missing-pipeline404 `{}` handling; native UID1000 traversal; exact projector bulk coordinator/shard permissions without broad write/delete grants; disabled all five Query Insights settings after an attributed async export-index change. A later native ML config-index appearance raced snapshots: native named-index health now waits for an active shard, since stock yellow can still mean zero active primaries. First response captured privately; no arbitrary sleep or rebaseline. Stock Security REST denial does not necessarily name the user; tests now require its actual authorization signature and unchanged state, not an invented username echo. Original denial bodies retained privately on failure; no raw payload logging. Every failed synthetic scope was reconciled and cleaned by exact owner, not treated as rollback.

Implementer `c4e61245-88e4-4d84-b05f-86602bde63cd`; tester `df4e9db1-305a-4935-8ce2-b4c73e96271d`. Earlier reviewer `9b78abf6-f817-4ccc-8d03-0827d958a233`; native readiness/denial source reviewer `46eefd92-2130-4db4-86c7-d23599ab42c0`. Final independent reviewer `a5551cff-1156-41fb-acca-4a1acef6201d` accepted **correctness and simplicity**, ran all180 units and one exact final rehearsal, verified unchanged production scope and cleanup. Final integrated docs review passed without blockers; tested source manifest unchanged. Integrator reran all180 units, CLI help and staged whitespace checks successfully. All exposed GPT-6-Astra; no selectable Terra. Tester success included reporting-only first-health preservation; independent repeat used the exact documented CLI. A subsequent tester verification was interrupted and not credited; independent checks supersede that evidence gap.

Limits: stock3.1.0 is **unmaintained compatibility-only**, not an approved live pin. Rust-client CA inputs/tests are next; no SDK fork planned. Operator verification uses admin credentials, not a restricted verifier. No active CDC/application flow, expired-certificate/rotation, host/daemon reboot or live-dev acceptance. Native upload is separate/non-atomic; existing Query Insights exporter changes may delete old insight indices. Maintained engine, actual endpoints/certificates/five runtime credentials, application CA wiring and setup authority remain gates. Removing unused tooling changes no runtime; after use it cannot undo security/data. Reproduction and protected setup: `deploy/tests/README.md`, `deploy/compose/README.md`, `deploy/bin/README.md`.

## Rust OpenSearch CA wiring — isolated acceptance

Baseline `14fa8e7d80841d34ce69dd51e77a98e51a213c67`, clean `task/#1412-deployment` before work. Accepted capability commit `9ed92785c2d2fb6a88558458259b40c6612c3121` (`feat(deploy): load verified OpenSearch CA in native clients`). Code/final docs independently accepted; staged whitespace passed; final Docker inventory and TLS temp checks empty. Existing Git gc warnings remain untouched. API, worker SDK/direct preflight and cron now use shared `platform-opensearch::tls`: dev/prod HTTPS + `OPENSEARCH_SSL_ROOT_CERT`, explicit local/test/ephemeral preserve optional CA/HTTP; supplied CA with HTTP rejects. Bounded certificate-only PEM bundle validated/frozen before dependency I/O, errors redacted, full CA/hostname checks and additive built-in trust. No environment lookup/domain logic in platform crate. Architecture §§3.7/16 and three depgraph edges document this small, immediately consumed expansion; no new package versions, SDK fork, migrations or framework.

Ordinary Compose adds public `opensearch-ca.pem` read-only for API/both slots, cron and three search workers only. Existing host command snapshots/checks the file and exact real-stage path; no new state machinery. Actual Compose render proves candidate inheritance. Cached networkless UID10001 helper reads exact public CA bytes; write returns EROFS; exact-owned cleanup confirmed. Existing installations require deliberate flock-held tooling/mount convergence; historical images must be rebuilt, not relabelled.

Independent reviewer ran bounded locked/offline workspace check, four-crate all-target/all-feature check, dependency graph, formatting, API375/worker269/cron71/platform12 library tests and worker34 binary tests: **761 passed**, six ignored parent-owned/opt-in entries. Owners invoke required fixture children; ignored count is not blanket acceptance. Full deployment Python suite with both opt-ins: **184 passed, zero skips**. Real loopback TLS exercises all four production constructors, auth, frozen trust and negative peers; worker/cron cases require certificate alerts, not timeout/reset. Shared two-CA overlap and direct no-redirect tests pass. No validation timeout or unrelated failure reported. Existing isolated domain/LocalStack/full-workspace library suites not run.

Shared implementer `880ad909-dba0-4ec8-81f1-383d3fda7306`, worker `34496ed8-d9ed-4e69-84e1-809535d77604`, API/cron `3b2dcef1-d9c0-4085-a47a-dc76c1966965`, runtime/mount tester `8221a511-a7dc-4d1a-ac09-5a780d14b65c`. Independent reviewer `fc482df0-d2fd-4ef0-80b3-b903a648f08e` accepted **correctness and simplicity**, repeated checks/tests, verified cleanup and exactly10 lock dependency edges/no package identity changes. Final docs signoff confirmed counts, redirect limits, rebuild and already-adopted convergence gates. Integrator independently reran all761 Rust tests/all184 Python tests, workspace formatting and dependency rules successfully. All exposed GPT-6-Astra, no Terra selector. Corrected initial helper local-only policy to preserve all three local stages; fixed test URL normalization, initial API TLS fixture, missing depgraph edges and witness join-on-drop. One Python expected input count updated14→15 for the new CA; no gate weakened. Lockfile writes appeared during delegated work despite locked-only handoffs; original writer unverified, integrator inspected/accepted the exact10 required edges without version changes.

Limits: no image rebuild/push/live activation, full secured-node application startup, domain CDC/custody or reboot test. Stock3.1 compatibility fixture remains unmaintained. SDK2.4 lacks redirect customization: destination/scheme changes, including HTTPS→HTTP and replayed bodies, remain possible despite initial HTTPS and auth stripping. Use a trusted nonredirecting endpoint; direct preflight rejects redirects. No claim of exclusive CA pinning, automatic rotation or HTTPS on every SDK hop. Only public CA goes into apps; rotate with controlled restart. Reverting loses private-CA support—never compensate with insecure TLS or real-stage HTTP. No data/queue/schema rollback. Operator/reproduction docs: `deploy/compose/README.md`, `deploy/bin/README.md`, `deploy/tests/README.md`.

## External and test gates

Host target and preferred AWS region are now explicit. AWS access and legacy local retirement are verified. Legacy AWS teardown is complete. Remaining: approved scoped identity and new host setup; endpoint/DNS/CA, firewall policy/authority, actual runtime credentials and real-stage initialization inputs. Existing GitHub dev reviewer policy is observed, not automatically approved for the replacement hybrid flow. R5–R8 live acceptance remains blocked. Existing source/template observations never prove a running deployment.

R2 images, R3 empty-stack and bounded R4 idle A→B/bad-candidate gates are cleared by actual local execution. R3 live-security/provider and R4 active-custody/load/reboot gates remain above. R5 offline CDK wiring is tested; real AWS/PostgreSQL validation still needs explicit target account/region, host/CA/firewall inputs and operation-specific authority. No LocalStack Pro licensing/provider authority inferred. Prior full workspace library test timed out; not rerun for Compose/test/docs-only changes. No workflows newly enabled or dispatched; existing deployment workflow remains enabled.

## C1b — crawler test boundaries and CI evidence collection

Base head: `0b559537d87d7fbc8d1b96f295d8c6920f05030f`, clean before C1b. C1b implementation commit `01f58a18b3761f90b65224004079cd6ba43961c6` is now observed on the remote feature branch; the preceding local-only/unpushed wording was historical. It keeps C1/C1a production and deployment changes unchanged.

- The obsolete library source-location assertion no longer searches thin `server.rs` for database validation before `aws_config::defaults`. The remaining server-entrypoint check only rejects bootstrap/pool bypass text. Real `server_preflight` behavior now rejects malformed `LOCAL_DB_URL` and `BUSINESS_DATABASE_URL` in daemon and `--check-config` modes before either loopback database spy or CloudWatch spy activity. The executable also checks non-Unicode database/TLS/ambient-option input in both modes with redacted output. Local-development parser spelling remains checked only where the adapter is owned: the bootstrap runtime module and three demos.
- The two crawler-schema candidate persistence tests (`should_clear_product_fingerprints_on_removal_and_fence_stale_removal_after_restore` and `should_leave_due_progress_unchanged_when_local_mark_statement_fails`) now run in the discovered `src/crawler/tests/crawler_candidate_postgres.rs` executable. It uses the public candidate service API and a supplied `[7_u8; 32]` removal hash; the private raw-input constructor stays private. The library retains the pure candidate-selection assertion and business-schema `should_persist_cached_schema_image_evidence_in_raw_revision` persistence test. No migration SQL, `test-api`, production SQL/service behavior, or schema reset changed.
- `cargo metadata --no-deps --format-version 1` lists the new target. Local formatting, dependency rules, crawler all-target check, focused config tests (`74 passed`), both integration target `--no-run` builds, all 11 loopback `server_preflight` tests, the malformed-URL exact test, and the non-Unicode exact test pass. The real PostgreSQL candidate target/library/combined-order matrix was not run here because this workstation is not an explicitly authorized disposable fixture runner; no Docker/database execution is credited.
- Historical C1a run `35013433513`, attempt `1`, had actual merge checkout `b36a9d57e01f06d5a623b78b89f42e3027b435a5`: deployment tooling, workspace lint, CDK, MJML, and cron passed; crawler reached compilation and failed with `807 passed, 3 failed`. Canceled/incomplete jobs were not counted. C1b changes `.github/workflows/integrate.yml` to `fail-fast: false` without `continue-on-error`; no new `Integrate (CI)` run existed for this C1b head before the C1c merge. Remote PostgreSQL and complete-matrix acceptance remain pending an owner-authorized push and full job-page inspection.

C1b implementation is not remote-accepted. Deployment readiness remains **false**. No C2, OpenSearch upgrade, live operation, or deployment operation was performed.

## C1c — integrate current develop with deployment branch

2026-09-16: C1c starts from deployment head `01f58a18b3761f90b65224004079cd6ba43961c6`. Fresh fetch froze `origin/develop` at `b08dd4f3114ff27284336b22c30343e4ce25d3b5`, including accepted auctions `846e2d81b` and the later fetched dependency updates. The remote deployment ref matched the starting head. Ordinary `git merge --no-ff --no-commit` produced signed merge commit `64ad07d97565bb0621c94756c45e3c67c3399b79`; no force-push, PR merge/base change, GitHub-setting change, or live mutation occurred.

- Git reported exactly four conflicts: `depgraph-rules.toml`, `docs/CHANGELOG.md`, `src/crawler/AGENTS.md`, and `src/crawler/src/scraper/scraper_service/tests/hash_skip.rs`. Resolutions retained deployment dependency/startup/TLS rules and C1b custody/test boundaries while retaining the accepted Auction crates/routes, current event shape, current mappings, and crawler no-auction extraction boundary. API composition remains shared; worker remains the existing ten scopes with no auction queue/CDC consumer.
- The incoming lockfile/workspace union was retained without an unrestricted update. Fetched lock changes include `aws-sdk-s3` 1.145.0→1.146.1, `uuid` 1.26.0→1.26.1, Windows dependency shifts, and `html-to-markdown-rs` 3.12.3→3.13.0. The last value differs from the fetched Dependabot subject, which says 3.12.4; the fetched target and lockfile were not rewritten.
- Current Auction mapping bytes changed the reviewed OpenSearch inputs. The first lightweight run correctly failed with three `SOURCE_NOT_REVIEWED` errors; `deploy/tests/smoke-opensearch.py` was then updated only with the exact current hashes (`product_listings.json` `b75fe707…`, `user_search_filters.json` `b8996a4…`). The rerun passed all 198 discovered checks with exactly the one documented cached-helper skip. This is source-guard reconciliation, not an OpenSearch rehearsal or latest-engine acceptance.
- Local merged-tree checks passed: formatting, locked metadata, dependency rules, locked workspace all-target/all-feature check, and locked all-target/all-feature Clippy with warnings denied; crawler config tests `74 passed`, `server_preflight` `11 passed`, and `crawler_candidate_postgres --no-run` compiled the separate target. The lightweight deployment runner passed `198` tests, `0` failures/errors, `1` documented skip. The candidate target and full crawler library were not run against PostgreSQL on this workstation.
- No C1c `Integrate (CI)` run exists yet. The prior dynamic Code Quality run `35018920288` is separate evidence and is not PostgreSQL or complete-matrix acceptance. Authorized-runner PostgreSQL candidate/full crawler tests, affected auction/API/worker integration tests, and complete remote job-page inspection remain pending. Latest-engine OpenSearch, image rebuild/publication, cloud, host, live database/search, and deployment acceptance remain unperformed.

Independent review was unavailable after the delegated review session stopped; integrator self-review covered the four conflict files, shared API composition, ten worker scopes, current CDC validation, crawler no-auction boundary, separate candidate executable, source guards, and deployment workflow safeguards. Deployment readiness remains **false**. C1c stops here; C2 has not started.
