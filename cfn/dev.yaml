AWSTemplateFormatVersion: "2010-09-09"
Description: Development Stack for the Aura-Historia Application-Backend

Parameters:
  ArtifactBucket:
    Type: String
    Description: "S3 bucket containing Lambda deployment zips"
  ResourceBucket:
    Type: String
    Description: "S3 bucket containing resources"
  MailTemplateBucket:
    Type: String
    Description: "S3 bucket containing mail-templates"
  CommitSHA:
    Type: String
  Ec2KeyPairName:
    Type: AWS::EC2::KeyPair::KeyName
    Description: For EC2 SSH-Access
  OpenSearchEndpointUrl:
    Type: String
    Description: "URL for the self-hosted OpenSearch-Cluster"

Resources:
  PrimaryUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: "primary-userpool-dev"
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
          TemporaryPasswordValidityDays: 7
      UserPoolAddOns:
        AdvancedSecurityMode: ENFORCED
      VerificationMessageTemplate:
        DefaultEmailOption: CONFIRM_WITH_CODE
      EmailVerificationSubject: "Verify your email"
      EmailVerificationMessage: "Your verification code is {####}"
      Schema:
        - Name: email
          AttributeDataType: String
          Mutable: false
          Required: true
      LambdaConfig:
        PostConfirmation: !GetAtt PrimaryUserPoolPostConfirmationLambda.Arn

  PrimaryUserPoolPostConfirmationLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "cognito-primary-userpool-post-confirmation-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                  - dynamodb:GetItem
                Resource: !GetAtt TableOne.Arn
  PrimaryUserPoolPostConfirmationLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "cognito-primary-userpool-post-confirmation-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt PrimaryUserPoolPostConfirmationLambdaRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "cognito-post-confirmation-dev-${CommitSHA}.zip"
      MemorySize: 256
      Timeout: 5
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  AllowCognitoInvokePrimaryUserPoolPostConfirmationLambda:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref PrimaryUserPoolPostConfirmationLambda
      Principal: cognito-idp.amazonaws.com
      SourceArn: !GetAtt PrimaryUserPool.Arn

  PrimaryUserPoolClientPublic:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref PrimaryUserPool
      ClientName: "primary-userpool-client-public-dev"
      GenerateSecret: false # PKCE replaces client secret
      PreventUserExistenceErrors: ENABLED
      AllowedOAuthFlowsUserPoolClient: true
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      SupportedIdentityProviders:
        - COGNITO
      CallbackURLs:
        - http://localhost:3000/
        - https://aura-historia.com/
      LogoutURLs:
        - http://localhost:3000/
        - https://aura-historia.com/
      EnableTokenRevocation: true
      AccessTokenValidity: 1 # hours
      IdTokenValidity: 1 # hours
      RefreshTokenValidity: 30 # days
      ReadAttributes:
        - email
        - email_verified
  PrimaryUserPoolClientAdmin:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref PrimaryUserPool
      ClientName: "primary-userpool-client-admin-dev"
      GenerateSecret: false
      PreventUserExistenceErrors: ENABLED
      AllowedOAuthFlowsUserPoolClient: false
      ExplicitAuthFlows:
        - ALLOW_ADMIN_USER_PASSWORD_AUTH
      EnableTokenRevocation: true
      AccessTokenValidity: 1 # hours
      IdTokenValidity: 1 # hours
      RefreshTokenValidity: 30 # days
      ReadAttributes:
        - email
      WriteAttributes:
        - email
  PrimaryUserPoolDomain:
    Type: AWS::Cognito::UserPoolDomain
    Properties:
      Domain: "primary-userpool-dev" # hosted UI domain
      UserPoolId: !Ref PrimaryUserPool

  TableOne:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: "table_1-dev"
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: sk
          AttributeType: S
        - AttributeName: lsi1_sk
          AttributeType: S
        - AttributeName: gsi1_pk
          AttributeType: S
        - AttributeName: gsi1_sk
          AttributeType: S
        - AttributeName: gsi2_pk
          AttributeType: S
        - AttributeName: gsi2_sk
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
        - AttributeName: sk
          KeyType: RANGE
      LocalSecondaryIndexes:
        - IndexName: lsi1
          KeySchema:
            - AttributeName: pk
              KeyType: HASH
            - AttributeName: lsi1_sk
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
      GlobalSecondaryIndexes:
        - IndexName: gsi1
          KeySchema:
            - AttributeName: gsi1_pk
              KeyType: HASH
            - AttributeName: gsi1_sk
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: gsi2
          KeySchema:
            - AttributeName: gsi2_pk
              KeyType: HASH
            - AttributeName: gsi2_sk
              KeyType: RANGE
          Projection:
            ProjectionType: KEYS_ONLY
      BillingMode: PAY_PER_REQUEST
      TableClass: STANDARD
      StreamSpecification:
        StreamViewType: NEW_IMAGE

  DynamoDbEventBus:
    Type: AWS::Events::EventBus
    Properties:
      Name: "dynamodb-event-bus-dev"
  PipeRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "dynamodb-to-eventbridge-pipe-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: pipes.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: PipePolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:DescribeStream
                  - dynamodb:GetRecords
                  - dynamodb:GetShardIterator
                  - dynamodb:ListStreams
                Resource: "*"
              - Effect: Allow
                Action:
                  - events:PutEvents
                Resource: !GetAtt DynamoDbEventBus.Arn
  TableOneStreamToEventBusPipe:
    Type: AWS::Pipes::Pipe
    Properties:
      Name: "tableone-stream-to-eventbus-dev"
      RoleArn: !GetAtt PipeRole.Arn
      Source: !GetAtt TableOne.StreamArn
      SourceParameters:
        DynamoDBStreamParameters:
          StartingPosition: LATEST
          BatchSize: 10
          MaximumBatchingWindowInSeconds: 1
        FilterCriteria:
          Filters:
            - Pattern: |
                {
                  "eventName": ["INSERT"],
                  "dynamodb": {
                    "NewImage": {
                      "pk": {
                        "S": [{ "prefix": "product#shop_id#" }]
                      },
                      "sk": {
                        "S": [{ "prefix": "product#event#" }]
                      }
                    }
                  }
                }
            - Pattern: |
                {
                  "eventName": ["MODIFY"],
                  "dynamodb": {
                    "NewImage": {
                      "pk": {
                        "S": [{ "prefix": "user#" }]
                      },
                      "sk": {
                        "S": [ "user#details" ]
                      }
                    }
                  }
                }
            - Pattern: |
                {
                  "eventName": ["INSERT", "MODIFY"],
                  "dynamodb": {
                    "NewImage": {
                      "pk": {
                        "S": [{ "prefix": "shop#shop_id#" }]
                      },
                      "sk": {
                        "S": [ "shop#details" ]
                      }
                    }
                  }
                }
      Target: !GetAtt DynamoDbEventBus.Arn
      TargetParameters:
        EventBridgeEventBusParameters:
          DetailType: "DynamoDBStreamRecord"
          Source: !Ref TableOne

  DynamoDbEventBusQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref ProductMaterializeDynamoDbNewQ
        - !Ref ProductMaterializeDynamoDbUpdateQ
        - !Ref ProductMaterializeOpenSearchNewQ
        - !Ref ProductMaterializeOpenSearchUpdateQ
        - !Ref ShopOpenSearchIndexQ
        - !Ref ProductUpdateNotifyUserQ
        - !Ref ProductPipelineInitQ
        - !Ref UserLambdaFanoutUpdateWatchlistQ
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: events.amazonaws.com
            Action: sqs:SendMessage
            Resource:
              - !GetAtt ProductMaterializeDynamoDbNewQ.Arn
              - !GetAtt ProductMaterializeDynamoDbUpdateQ.Arn
              - !GetAtt ProductMaterializeOpenSearchNewQ.Arn
              - !GetAtt ProductMaterializeOpenSearchUpdateQ.Arn
              - !GetAtt ShopOpenSearchIndexQ.Arn
              - !GetAtt ProductUpdateNotifyUserQ.Arn
              - !GetAtt ProductPipelineInitQ.Arn
              - !GetAtt UserLambdaFanoutUpdateWatchlistQ.Arn

  Api:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: "api-dev"
      ProtocolType: HTTP
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - "*"
        AllowHeaders:
          - Authorization
          - Content-Type
          - Accept-Language
          - Accept
  ApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      StageName: "dev"
      ApiId: !Ref Api
      AutoDeploy: true
      DefaultRouteSettings:
        ThrottlingBurstLimit: 50
        ThrottlingRateLimit: 20
  ApiCognitoAuthorizer:
    Type: AWS::ApiGatewayV2::Authorizer
    Properties:
      ApiId: !Ref Api
      Name: CognitoJwtAuthorizer
      AuthorizerType: JWT
      IdentitySource:
        - "$request.header.Authorization"
      JwtConfiguration:
        Issuer: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${PrimaryUserPool}"
        Audience:
          - !Ref PrimaryUserPoolClientPublic
          - !Ref PrimaryUserPoolClientAdmin


  ProductApiGetProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/products/{shopId}/{shopsProductId}"
      Target: !Sub "integrations/${ProductApiLambdaIntegration}"
  ProductApiGetProductBySlugIdRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/products/by-slug/{shopSlugId}/{productSlugId}"
      Target: !Sub "integrations/${ProductApiLambdaIntegration}"
  ProductApiGetProductSimilarRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/products/{shopId}/{shopsProductId}/similar"
      Target: !Sub "integrations/${ProductApiLambdaIntegration}"
  ProductApiPutProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "PUT /api/v1/products"
      Target: !Sub "integrations/${ProductApiLambdaIntegration}"
  ProductApiSearchProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "POST /api/v1/products/search"
      Target: !Sub "integrations/${ProductApiLambdaIntegration}"
  ProductApiLambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref Api
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub "arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${ProductApiLambda}"
      PayloadFormatVersion: "2.0"
  ProductApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-api-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:Query
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:TransactGetItems
                  - dynamodb:TransactWriteItems
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
  ProductApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-api-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductApiRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: "product-api-dev-latest.zip"
      MemorySize: 512
      Timeout: 10
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          OPENSEARCH_ENDPOINT_URL: !Ref OpenSearchEndpointUrl
          STAGE: "dev"
          OPENSEARCH_USERNAME: "{{resolve:ssm:/opensearch/dev/username}}"
          OPENSEARCH_PASSWORD: "{{resolve:ssm:/opensearch/dev/password}}"
          DYNAMODB_TABLE_NAME: !Ref TableOne
          USER_POOL_ID: !Ref PrimaryUserPool
          USER_POOL_PUBLIC_CLIENT_ID: !Ref PrimaryUserPoolClientPublic
          USER_POOL_ADMIN_CLIENT_ID: !Ref PrimaryUserPoolClientAdmin
  ProductApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ProductApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*/*/api/v1/products*"

  ProductWatchlistApiDeleteWatchlistProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "DELETE /api/v1/me/watchlist/{shopId}/{shopsProductId}"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${ProductWatchlistApiLambdaIntegration}"
  ProductWatchlistApiGetAllWatchlistProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/me/watchlist"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${ProductWatchlistApiLambdaIntegration}"
  ProductWatchlistApiPatchWatchlistProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "PATCH /api/v1/me/watchlist/{shopId}/{shopsProductId}"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${ProductWatchlistApiLambdaIntegration}"
  ProductWatchlistApiPostWatchlistProductRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "POST /api/v1/me/watchlist"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${ProductWatchlistApiLambdaIntegration}"
  ProductWatchlistApiLambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref Api
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub "arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${ProductWatchlistApiLambda}"
      PayloadFormatVersion: "2.0"
  ProductWatchlistApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-watchlist-api-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:Query
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
  ProductWatchlistApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-watchlist-api-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductWatchlistApiRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: "product-watchlist-api-dev-latest.zip"
      MemorySize: 256
      Timeout: 10
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  ProductWatchlistApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ProductWatchlistApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*/*/api/v1/me/watchlist*"

  UserApiGetUserAccountRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/me/account"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${UserApiLambdaIntegration}"
  UserApiPatchUserAccountRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "PATCH /api/v1/me/account"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${UserApiLambdaIntegration}"
  UserApiLambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref Api
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub "arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${UserApiLambda}"
      PayloadFormatVersion: "2.0"
  UserApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "user-api-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:UpdateItem
                Resource: !GetAtt TableOne.Arn
  UserApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "user-api-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt UserApiRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: "user-api-dev-latest.zip"
      MemorySize: 256
      Timeout: 10
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  UserApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref UserApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*/*/api/v1/me/account*"

  ShopApiGetShopByIdentifierRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/shops/{shopIdentifier}"
      Target: !Sub "integrations/${ShopApiLambdaIntegration}"
  ShopApiGetShopBySlugIdRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/shops/by-slug/{shopSlugId}"
      Target: !Sub "integrations/${ShopApiLambdaIntegration}"
  ShopApiPatchShopRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "PATCH /api/v1/shops/{shopIdentifier}"
      Target: !Sub "integrations/${ShopApiLambdaIntegration}"
  ShopApiPostShopRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "POST /api/v1/shops"
      Target: !Sub "integrations/${ShopApiLambdaIntegration}"
  ShopApiSearchRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "POST /api/v1/shops/search"
      Target: !Sub "integrations/${ShopApiLambdaIntegration}"
  ShopApiLambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref Api
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub "arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${ShopApiLambda}"
      PayloadFormatVersion: "2.0"
  ShopApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "shop-api-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:Query
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:TransactGetItems
                  - dynamodb:TransactWriteItems
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
  ShopApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "shop-api-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ShopApiRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: "shop-api-dev-latest.zip"
      MemorySize: 512
      Timeout: 10
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
          OPENSEARCH_ENDPOINT_URL: !Ref OpenSearchEndpointUrl
          OPENSEARCH_USERNAME: "{{resolve:ssm:/opensearch/dev/username}}"
          OPENSEARCH_PASSWORD: "{{resolve:ssm:/opensearch/dev/password}}"
  ShopApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ShopApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*/*/api/v1/shops/*"

  SearchFilterApiDeleteSearchFilterRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "DELETE /api/v1/me/search-filters/{userSearchFilterId}"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${SearchFilterApiLambdaIntegration}"
  SearchFilterApiGetOneSearchFilterRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/me/search-filters/{userSearchFilterId}"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${SearchFilterApiLambdaIntegration}"
  SearchFilterApiGetAllSearchFilterRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "GET /api/v1/me/search-filters"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${SearchFilterApiLambdaIntegration}"
  SearchFilterApiPatchSearchFilterRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "PATCH /api/v1/me/search-filters/{userSearchFilterId}"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${SearchFilterApiLambdaIntegration}"
  SearchFilterApiPostSearchFilterRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref Api
      RouteKey: "POST /api/v1/me/search-filters"
      AuthorizationType: JWT
      AuthorizerId: !Ref ApiCognitoAuthorizer
      Target: !Sub "integrations/${SearchFilterApiLambdaIntegration}"
  SearchFilterApiLambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref Api
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub "arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${SearchFilterApiLambda}"
      PayloadFormatVersion: "2.0"
  SearchFilterApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "search-filter-api-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:Query
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
  SearchFilterApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "search-filter-api-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt SearchFilterApiRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: "search-filter-api-dev-latest.zip"
      MemorySize: 512
      Timeout: 10
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  SearchFilterApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref SearchFilterApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*/*/api/v1/me/search-filters*"

  SendMailDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "mail-lambda-send-dev"
      MessageRetentionPeriod: 1209600
  SendMailQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "mail-lambda-send-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt SendMailDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 180
  SendMailRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "mail-lambda-send-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:TransactGetItems
                  - dynamodb:TransactWriteItems
                Resource:
                  - !GetAtt TableOne.Arn
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt SendMailQ.Arn
        - PolicyName: SESSendMailAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - ses:SendEmail
                  - ses:SendRawEmail
                Resource: "*"
        - PolicyName: S3Access
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                Resource: !Sub "arn:aws:s3:::${MailTemplateBucket}/*"
  SendMailLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "mail-lambda-send-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt SendMailRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "mail-lambda-send-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
          S3_BUCKET_NAME_TEMPLATES: !Ref MailTemplateBucket
          STAGE_NAME: "dev"
          COMMIT_SHA: !Sub "${CommitSHA}"
  SendMailMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref SendMailLambda
      EventSourceArn: !GetAtt SendMailQ.Arn
      Enabled: true
      BatchSize: 25
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  ProductMaterializeDynamoDbNewDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-dynamodb-new-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductMaterializeDynamoDbNewQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-dynamodb-new-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductMaterializeDynamoDbNewDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ProductMaterializeDynamoDbNewRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-lambda-materialize-dynamodb-new-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:*
                Resource: !GetAtt TableOne.Arn
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ProductMaterializeDynamoDbNewQ.Arn
  ProductMaterializeDynamoDbNewLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-lambda-materialize-dynamodb-new-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductMaterializeDynamoDbNewRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-lambda-materialize-dynamodb-new-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  ProductMaterializeDynamoDbNewMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ProductMaterializeDynamoDbNewLambda
      EventSourceArn: !GetAtt ProductMaterializeDynamoDbNewQ.Arn
      Enabled: true
      BatchSize: 200
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  ProductMaterializeDynamoDbUpdateDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-dynamodb-update-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductMaterializeDynamoDbUpdateQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-dynamodb-update-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductMaterializeDynamoDbUpdateDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ProductMaterializeDynamoDbUpdateRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-lambda-materialize-dynamodb-update-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:*
                Resource: !GetAtt TableOne.Arn
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ProductMaterializeDynamoDbUpdateQ.Arn
  ProductMaterializeDynamoDbUpdateLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-lambda-materialize-dynamodb-update-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductMaterializeDynamoDbUpdateRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-lambda-materialize-dynamodb-update-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  ProductMaterializeDynamoDbUpdateMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ProductMaterializeDynamoDbUpdateLambda
      EventSourceArn: !GetAtt ProductMaterializeDynamoDbUpdateQ.Arn
      Enabled: true
      BatchSize: 200
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  ProductMaterializeOpenSearchNewDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-opensearch-new-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductMaterializeOpenSearchNewQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-opensearch-new-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductMaterializeOpenSearchNewDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ProductMaterializeOpenSearchNewRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-lambda-materialize-opensearch-new-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ProductMaterializeOpenSearchNewQ.Arn
  ProductMaterializeOpenSearchNewLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-lambda-materialize-opensearch-new-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductMaterializeOpenSearchNewRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-lambda-materialize-opensearch-new-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          STAGE: "dev"
          OPENSEARCH_ENDPOINT_URL: !Ref OpenSearchEndpointUrl
          OPENSEARCH_USERNAME: "{{resolve:ssm:/opensearch/dev/username}}"
          OPENSEARCH_PASSWORD: "{{resolve:ssm:/opensearch/dev/password}}"
  ProductMaterializeOpenSearchDbNewMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ProductMaterializeOpenSearchNewLambda
      EventSourceArn: !GetAtt ProductMaterializeOpenSearchNewQ.Arn
      Enabled: true
      BatchSize: 200
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  ProductMaterializeOpenSearchUpdateDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-opensearch-update-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductMaterializeOpenSearchUpdateQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-materialize-opensearch-update-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductMaterializeOpenSearchUpdateDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ProductMaterializeOpenSearchUpdateRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-lambda-materialize-opensearch-update-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ProductMaterializeOpenSearchUpdateQ.Arn
  ProductMaterializeOpenSearchUpdateLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-lambda-materialize-opensearch-update-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductMaterializeOpenSearchUpdateRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-lambda-materialize-opensearch-update-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          STAGE: "dev"
          OPENSEARCH_ENDPOINT_URL: !Ref OpenSearchEndpointUrl
          OPENSEARCH_USERNAME: "{{resolve:ssm:/opensearch/dev/username}}"
          OPENSEARCH_PASSWORD: "{{resolve:ssm:/opensearch/dev/password}}"
  ProductMaterializeOpenSearchDbUpdateMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ProductMaterializeOpenSearchUpdateLambda
      EventSourceArn: !GetAtt ProductMaterializeOpenSearchUpdateQ.Arn
      Enabled: true
      BatchSize: 2500
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  ShopOpenSearchIndexDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "shop-opensearch-index-dlq-dev"
      MessageRetentionPeriod: 1209600
  ShopOpenSearchIndexQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "shop-opensearch-index-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ShopOpenSearchIndexDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ShopOpenSearchIndexRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "shop-opensearch-index-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ShopOpenSearchIndexQ.Arn
  ShopOpenSearchIndexLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "shop-opensearch-index-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ShopOpenSearchIndexRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "shop-lambda-opensearch-index-dev-${CommitSHA}.zip"
      MemorySize: 256
      Timeout: 30
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          OPENSEARCH_ENDPOINT_URL: !Ref OpenSearchEndpointUrl
          OPENSEARCH_USERNAME: "{{resolve:ssm:/opensearch/dev/username}}"
          OPENSEARCH_PASSWORD: "{{resolve:ssm:/opensearch/dev/password}}"
          STAGE: "dev"
  ShopOpenSearchIndexMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ShopOpenSearchIndexLambda
      EventSourceArn: !GetAtt ShopOpenSearchIndexQ.Arn
      Enabled: true
      BatchSize: 1
      MaximumBatchingWindowInSeconds: 0

  ProductUpdateNotifyUserDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-update-notify-user-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductUpdateNotifyUserQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-lambda-update-notify-user-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductUpdateNotifyUserDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  ProductUpdateNotifyUserRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-lambda-update-notify-user-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:Query
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt ProductUpdateNotifyUserQ.Arn
        - PolicyName: SQSSendMessage
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                Resource: !GetAtt SendMailQ.Arn
  ProductUpdateNotifyUserLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-lambda-update-notify-user-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductUpdateNotifyUserRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-lambda-update-notify-user-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
          MAIL_QUEUE_URL: !Ref SendMailQ
          SENDER_MAIL: "no-reply@aura-historia.com"
  ProductUpdateNotifyUserMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref ProductUpdateNotifyUserLambda
      EventSourceArn: !GetAtt ProductUpdateNotifyUserQ.Arn
      Enabled: true
      BatchSize: 10
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  UserLambdaFanoutUpdateWatchlistDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "user-lambda-fanout-update-watchlist-dlq-dev"
      MessageRetentionPeriod: 1209600
  UserLambdaFanoutUpdateWatchlistQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "user-lambda-fanout-update-watchlist-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt UserLambdaFanoutUpdateWatchlistDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 360
  UserLambdaFanoutUpdateWatchlistRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "user-lambda-fanout-update-watchlist-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:Query
                  - dynamodb:PutItem
                  - dynamodb:DeleteItem
                  - dynamodb:UpdateItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:TransactGetItems
                  - dynamodb:TransactWriteItems
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                Resource: !GetAtt UserLambdaFanoutUpdateWatchlistQ.Arn
  UserLambdaFanoutUpdateWatchlistLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "user-lambda-fanout-update-watchlist-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt UserLambdaFanoutUpdateWatchlistRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "user-lambda-fanout-update-watchlist-dev-${CommitSHA}.zip"
      MemorySize: 512
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
  UserLambdaFanoutUpdateWatchlistMapping:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref UserLambdaFanoutUpdateWatchlistLambda
      EventSourceArn: !GetAtt UserLambdaFanoutUpdateWatchlistQ.Arn
      Enabled: true
      BatchSize: 10
      MaximumBatchingWindowInSeconds: 1
      FunctionResponseTypes:
        - ReportBatchItemFailures

  DynamoDbProductEventRecordCreatedMaterializeDynamoDbProductRecordEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-mat-ddb-new-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - "CREATED"
      Targets:
        - Id: ProductMaterializeDynamoDbNewQ
          Arn: !GetAtt ProductMaterializeDynamoDbNewQ.Arn
  DynamoDbProductEventRecordUpdatedMaterializeDynamoDbProductRecordEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-mat-ddb-update-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - prefix: "PRICE_"
                  - prefix: "STATE_"
      Targets:
        - Id: ProductMaterializeDynamoDbUpdateQ
          Arn: !GetAtt ProductMaterializeDynamoDbUpdateQ.Arn
  DynamoDbProductEventRecordCreatedMaterializeOpenSearchProductDocumentEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-mat-os-new-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - "CREATED"
      Targets:
        - Id: ProductMaterializeOpenSearchNewQ
          Arn: !GetAtt ProductMaterializeOpenSearchNewQ.Arn
  DynamoDbProductEventRecordUpdatedMaterializeOpenSearchProductDocumentEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-mat-os-update-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - prefix: "PRICE_"
                  - prefix: "STATE_"
      Targets:
        - Id: ProductMaterializeOpenSearchUpdateQ
          Arn: !GetAtt ProductMaterializeOpenSearchUpdateQ.Arn
  DynamoDbProductEventRecordUpdatedNotifyUserEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-update-notify-user-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - prefix: "PRICE_"
                  - prefix: "STATE_"
      Targets:
        - Id: ProductUpdateNotifyUserQ
          Arn: !GetAtt ProductUpdateNotifyUserQ.Arn
  DynamoDbProductEventRecordCreatedProductPipelineInitEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-product-pipeline-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
          dynamodb:
            NewImage:
              event_type:
                S:
                  - "CREATED"
      Targets:
        - Id: ProductPipelineInitQ
          Arn: !GetAtt ProductPipelineInitQ.Arn
  DynamoDbPutShopRecordEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-put-shop-record-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "INSERT"
            - "MODIFY"
          dynamodb:
            NewImage:
              sk:
                S:
                  - "shop#details"
      Targets:
        - Id: ShopOpenSearchIndexQ
          Arn: !GetAtt ShopOpenSearchIndexQ.Arn
  DynamoDbUserUpdatedFanoutWatchlistUpdateEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: "ddb-user-update-fanout-watchlist-dev"
      EventBusName: !Ref DynamoDbEventBus
      EventPattern:
        source:
          - !Ref TableOne
        detail-type:
          - "DynamoDBStreamRecord"
        detail:
          eventName:
            - "MODIFY"
          dynamodb:
            NewImage:
              sk:
                S:
                  - "user#details"
      Targets:
        - Id: UserLambdaFanoutUpdateWatchlistQ
          Arn: !GetAtt UserLambdaFanoutUpdateWatchlistQ.Arn

  ProductPipelineVpc:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.0.0.0/16
      EnableDnsSupport: true
      EnableDnsHostnames: true
      Tags:
        - Key: Name
          Value: "product-pipeline-vpc-dev"
  ProductPipelineInternetGateway:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
        - Key: Name
          Value: "product-pipeline-igw-dev"
  ProductPipelineVpcGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      InternetGatewayId: !Ref ProductPipelineInternetGateway
      VpcId: !Ref ProductPipelineVpc
  ProductPipelinePublicRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref ProductPipelineVpc
      Tags:
        - Key: Name
          Value: "product-pipeline-public-rt-dev"
  ProductPipelinePublicRoute:
    Type: AWS::EC2::Route
    DependsOn: ProductPipelineVpcGatewayAttachment
    Properties:
      RouteTableId: !Ref ProductPipelinePublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref ProductPipelineInternetGateway
  ProductPipelineSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ProductPipelineVpc
      CidrBlock: 10.0.1.0/24
      MapPublicIpOnLaunch: true
      AvailabilityZone: !Select [0, !GetAZs ""]
      Tags:
        - Key: Name
          Value: "product-pipeline-subnet-a-dev"
  ProductPipelineSubnetRouteAssocA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref ProductPipelineSubnetA
      RouteTableId: !Ref ProductPipelinePublicRouteTable
  ProductPipelineSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow SSH and internal comms
      VpcId: !Ref ProductPipelineVpc
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: "product-pipeline-sg-dev"

  ProductPipelineInitDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-init-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductPipelineInitQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-init-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductPipelineInitDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 60
  ProductPipelineInitRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-init-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
          - Effect: Allow
            Principal:
              Service: spot.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: AllowTerminateInASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: autoscaling:TerminateInstanceInAutoScalingGroup
                Resource: "*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                  - sqs:SendMessageBatch
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:DeleteMessageBatch
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - sqs:ChangeMessageVisibility
                Resource:
                  - !GetAtt ProductPipelineInitQ.Arn
                  - !GetAtt ProductPipelineTranslateQ.Arn
        - PolicyName: CloudWatchLogsCreate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
  ProductPipelineInitInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: "product-pipeline-init-profile-dev"
      Roles:
        - !Ref ProductPipelineInitRole
  ProductPipelineInitLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: "product-pipeline-init-template-dev"
      LaunchTemplateData:
        KeyName: !Ref Ec2KeyPairName
        IamInstanceProfile:
          Name: !Ref ProductPipelineInitInstanceProfile
        InstanceType: c7i.large
        ImageId: ami-004e960cde33f9146
        SecurityGroupIds: [!Ref ProductPipelineSecurityGroup]
        BlockDeviceMappings:
          - DeviceName: /dev/sda1
            Ebs:
              VolumeSize: 50
              VolumeType: gp3
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: "product-pipeline-init-dev"
              - Key: Fleet
                Value: product-pipeline-init
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail
            sudo -i
            export STAGE="dev"
            export STAGE_NAME=dev
            export CommitSHA="${CommitSHA}"
            export ARTIFACT_BUCKET="${ArtifactBucket}"
            export SOURCE_QUEUE_URL="${ProductPipelineInitQ}"
            export TARGET_QUEUE_URL="${ProductPipelineTranslateQ}"
            # Install awscli
            apt -y install unzip
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
            # Install cloudwatch-agent
            sudo wget https://s3.amazonaws.com/amazoncloudwatch-agent/debian/amd64/latest/amazon-cloudwatch-agent.deb
            sudo dpkg -i -E ./amazon-cloudwatch-agent.deb
            # Download and install binary
            sudo aws s3 cp "s3://${ArtifactBucket}/dev/${CommitSHA}/product-pipeline-init" /opt/product-pipeline-init --region eu-central-1
            sudo chmod +x /opt/product-pipeline-init
            # Configure CloudWatchAgent
            cat << EOF | sudo tee /opt/aws/amazon-cloudwatch-agent.json > /dev/null
            {
              "metrics": {
                "namespace": "CWAgent",
                "metrics_collected": {
                  "cpu": {
                    "measurement": [
                      {
                        "name": "cpu_usage_idle",
                        "rename": "CPU_IDLE",
                        "unit": "Percent"
                      },
                      {
                        "name": "cpu_usage_iowait",
                        "rename": "CPU_IOWAIT",
                        "unit": "Percent"
                      },
                      "cpu_time_guest"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"],
                    "totalcpu": false
                  },
                  "disk": {
                    "measurement": [
                      {
                        "name": "used_percent",
                        "rename": "DISK_USED_PERCENT",
                        "unit": "Percent"
                      },
                      "used",
                      "free"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  },
                  "diskio": {
                    "measurement": [
                      "io_time",
                      "read_bytes",
                      "write_bytes",
                      "reads",
                      "writes"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  },
                  "mem": {
                    "measurement": [
                      {
                        "name": "mem_used_percent",
                        "rename": "MEM_USED_PERCENT",
                        "unit": "Percent"
                      },
                      "mem_used",
                      "mem_available"
                    ],
                    "metrics_collection_interval": 60
                  },
                  "netstat": {
                    "measurement": [
                      "tcp_established",
                      "tcp_time_wait"
                    ],
                    "metrics_collection_interval": 60
                  },
                  "net": {
                    "measurement": [
                      "bytes_sent",
                      "bytes_recv",
                      "packets_sent",
                      "packets_recv"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  }
                }
              },
              "logs": {
                "logs_collected": {
                  "files": {
                    "collect_list": [
                      {
                        "file_path": "/var/log/product-pipeline-init.log",
                        "log_group_name": "/aws/ec2/$STAGE-product-pipeline-init",
                        "log_stream_name": "{instance_id}",
                        "timestamp_format": "%Y-%m-%dT%H:%M:%S.%f%z",
                        "timezone": "UTC",
                        "multi_line_start_pattern": "^{"
                      }
                    ]
                  }
                }
              }
            }
            EOF
            amazon-cloudwatch-agent-ctl -a fetch-config -m ec2 -c file:/opt/aws/amazon-cloudwatch-agent.json -s
            LOG_PATH="/var/log/product-pipeline-init.log"
            sudo touch "$LOG_PATH"
            sudo chmod 666 "$LOG_PATH"
            # Run binary
            echo "[$(date)] Running product-pipeline-init..." | tee -a "$LOG_PATH"
            /opt/product-pipeline-init >> "$LOG_PATH" 2>&1
            EXIT_CODE=$?
            echo "[$(date)] Job finished with code $EXIT_CODE" | tee -a "$LOG_PATH"
            sleep 5
            aws autoscaling terminate-instance-in-auto-scaling-group \
              --instance-id $(ec2metadata --instance-id) \
              --should-decrement-desired-capacity
            exit $EXIT_CODE
  ProductPipelineInitASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      VPCZoneIdentifier:
        - !Ref ProductPipelineSubnetA
      MinSize: 0
      MaxSize: 1
      DesiredCapacity: 0
      MixedInstancesPolicy:
        LaunchTemplate:
          LaunchTemplateSpecification:
            LaunchTemplateId: !Ref ProductPipelineInitLaunchTemplate
            Version: !GetAtt ProductPipelineInitLaunchTemplate.LatestVersionNumber
          Overrides:
            - InstanceType: c7i.large
            - InstanceType: c6i.large
        InstancesDistribution:
          OnDemandPercentageAboveBaseCapacity: 0
          SpotAllocationStrategy: price-capacity-optimized
      Tags:
        - Key: Name
          Value: "product-pipeline-init-dev"
          PropagateAtLaunch: true
      HealthCheckType: EC2
      HealthCheckGracePeriod: 120

  ProductPipelineTranslateDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-translate-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductPipelineTranslateQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-translate-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductPipelineTranslateDlq.Arn
        maxReceiveCount: 3
      VisibilityTimeout: 300
  ProductPipelineTranslateRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-translate-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
          - Effect: Allow
            Principal:
              Service: spot.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: AllowTerminateInASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: autoscaling:TerminateInstanceInAutoScalingGroup
                Resource: "*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                  - sqs:SendMessageBatch
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:DeleteMessageBatch
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - sqs:ChangeMessageVisibility
                Resource:
                  - !GetAtt ProductPipelineTranslateQ.Arn
                  - !GetAtt ProductPipelineEmbedTextQ.Arn
        - PolicyName: CloudWatchLogsCreate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
  ProductPipelineTranslateInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: "product-pipeline-translate-profile-dev"
      Roles:
        - !Ref ProductPipelineTranslateRole
  ProductPipelineTranslateLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: "product-pipeline-translate-template-dev"
      LaunchTemplateData:
        KeyName: !Ref Ec2KeyPairName
        IamInstanceProfile:
          Name: !Ref ProductPipelineTranslateInstanceProfile
        InstanceType: g6.xlarge # Placeholder - ASG overrides this
        ImageId: "{{resolve:ssm:/amis/dev/product-pipeline-translate/latest}}"
        SecurityGroupIds: [!Ref ProductPipelineSecurityGroup]
        BlockDeviceMappings:
          - DeviceName: /dev/sda1
            Ebs:
              VolumeSize: 80
              VolumeType: gp3
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: "product-pipeline-translate-dev"
              - Key: Fleet
                Value: product-pipeline-translate
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail
            sudo -i
            export STAGE="dev"
            export STAGE_NAME=dev
            export CommitSHA="${CommitSHA}"
            export ARTIFACT_BUCKET="${ArtifactBucket}"
            export SOURCE_QUEUE_URL="${ProductPipelineTranslateQ}"
            export TARGET_QUEUE_URL="${ProductPipelineEmbedTextQ}"
            # Download and install binary
            sudo aws s3 cp "s3://${ArtifactBucket}/dev/${CommitSHA}/product-pipeline-translate" /opt/product-pipeline-translate/product-pipeline-translate --region eu-central-1
            sudo chmod +x /opt/product-pipeline-translate/product-pipeline-translate
            # Prepare (preconfigured via custom ami) python venv
            cd /opt/product-pipeline-translate
            source .venv/bin/activate
            # Run binary
            LOG_PATH="/var/log/product-pipeline-translate.log"
            echo "[$(date)] Running product-pipeline-translate..." | tee -a "$LOG_PATH"
            ./product-pipeline-translate >> "$LOG_PATH" 2>&1
            EXIT_CODE=$?
            echo "[$(date)] Job finished with code $EXIT_CODE" | tee -a "$LOG_PATH"
            sleep 5
            aws autoscaling terminate-instance-in-auto-scaling-group \
              --instance-id $(ec2metadata --instance-id) \
              --should-decrement-desired-capacity
            exit $EXIT_CODE
  ProductPipelineTranslateASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      VPCZoneIdentifier:
        - !Ref ProductPipelineSubnetA
      MinSize: 0
      MaxSize: 1
      DesiredCapacity: 0
      MixedInstancesPolicy:
        LaunchTemplate:
          LaunchTemplateSpecification:
            LaunchTemplateId: !Ref ProductPipelineTranslateLaunchTemplate
            Version: !GetAtt ProductPipelineTranslateLaunchTemplate.LatestVersionNumber
          Overrides:
            - InstanceType: g6.xlarge
            - InstanceType: g6.2xlarge
            - InstanceType: g5.xlarge
            - InstanceType: g5.2xlarge
        InstancesDistribution:
          OnDemandPercentageAboveBaseCapacity: 0
          SpotAllocationStrategy: price-capacity-optimized
      Tags:
        - Key: Name
          Value: "product-pipeline-translate-dev"
          PropagateAtLaunch: true
      HealthCheckType: EC2
      HealthCheckGracePeriod: 120

  ProductPipelineEmbedTextDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-embed-text-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductPipelineEmbedTextQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-embed-text-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductPipelineEmbedTextDlq.Arn
        maxReceiveCount: 3
      VisibilityTimeout: 300
  ProductPipelineEmbedTextRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-embed-text-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
          - Effect: Allow
            Principal:
              Service: spot.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: AllowTerminateInASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: autoscaling:TerminateInstanceInAutoScalingGroup
                Resource: "*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                  - sqs:SendMessageBatch
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:DeleteMessageBatch
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - sqs:ChangeMessageVisibility
                Resource:
                  - !GetAtt ProductPipelineEmbedTextQ.Arn
                  - !GetAtt ProductPipelineExtractAttributeQ.Arn
        - PolicyName: CloudWatchLogsCreate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
  ProductPipelineEmbedTextInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: "product-pipeline-embed-text-profile-dev"
      Roles:
        - !Ref ProductPipelineEmbedTextRole
  ProductPipelineEmbedTextLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: "product-pipeline-embed-text-template-dev"
      LaunchTemplateData:
        KeyName: !Ref Ec2KeyPairName
        IamInstanceProfile:
          Name: !Ref ProductPipelineEmbedTextInstanceProfile
        InstanceType: g6.xlarge # Placeholder - ASG overrides this
        ImageId: "{{resolve:ssm:/amis/dev/product-pipeline-embed-text/latest}}"
        SecurityGroupIds: [!Ref ProductPipelineSecurityGroup]
        BlockDeviceMappings:
          - DeviceName: /dev/sda1
            Ebs:
              VolumeSize: 80
              VolumeType: gp3
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: "product-pipeline-embed-text-dev"
              - Key: Fleet
                Value: product-pipeline-embed-text
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail
            sudo -i
            export STAGE="dev"
            export STAGE_NAME=dev
            export CommitSHA="${CommitSHA}"
            export ARTIFACT_BUCKET="${ArtifactBucket}"
            export SOURCE_QUEUE_URL="${ProductPipelineEmbedTextQ}"
            export TARGET_QUEUE_URL="${ProductPipelineExtractAttributeQ}"
            # Download and install binary
            sudo aws s3 cp "s3://${ArtifactBucket}/dev/${CommitSHA}/product-pipeline-embed-text" /opt/product-pipeline-embed-text/product-pipeline-embed-text --region eu-central-1
            sudo chmod +x /opt/product-pipeline-embed-text/product-pipeline-embed-text
            # Prepare (preconfigured via custom ami) python venv
            cd /opt/product-pipeline-embed-text
            source .venv/bin/activate
            # Run binary
            LOG_PATH="/var/log/product-pipeline-embed-text.log"
            echo "[$(date)] Running product-pipeline-embed-text..." | tee -a "$LOG_PATH"
            ./product-pipeline-embed-text >> "$LOG_PATH" 2>&1
            EXIT_CODE=$?
            echo "[$(date)] Job finished with code $EXIT_CODE" | tee -a "$LOG_PATH"
            sleep 5
            aws autoscaling terminate-instance-in-auto-scaling-group \
              --instance-id $(ec2metadata --instance-id) \
              --should-decrement-desired-capacity
            exit $EXIT_CODE
  ProductPipelineEmbedTextASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      VPCZoneIdentifier:
        - !Ref ProductPipelineSubnetA
      MinSize: 0
      MaxSize: 1
      DesiredCapacity: 0
      MixedInstancesPolicy:
        LaunchTemplate:
          LaunchTemplateSpecification:
            LaunchTemplateId: !Ref ProductPipelineEmbedTextLaunchTemplate
            Version: !GetAtt ProductPipelineEmbedTextLaunchTemplate.LatestVersionNumber
          Overrides:
            - InstanceType: g6.xlarge
            - InstanceType: g6.2xlarge
            - InstanceType: g5.xlarge
            - InstanceType: g5.2xlarge
        InstancesDistribution:
          OnDemandPercentageAboveBaseCapacity: 0
          SpotAllocationStrategy: price-capacity-optimized
      Tags:
        - Key: Name
          Value: "product-pipeline-embed-text-dev"
          PropagateAtLaunch: true
      HealthCheckType: EC2
      HealthCheckGracePeriod: 120

  ProductPipelineExtractAttributeDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "product-pipeline-extract-attribute-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductPipelineExtractAttributeQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "product-pipeline-extract-attribute-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductPipelineExtractAttributeDlq.Arn
        maxReceiveCount: 3
      VisibilityTimeout: 300
  ProductPipelineExtractAttributeRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "product-pipeline-extract-attribute-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
          - Effect: Allow
            Principal:
              Service: spot.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: AllowTerminateInASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: autoscaling:TerminateInstanceInAutoScalingGroup
                Resource: "*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                  - sqs:SendMessageBatch
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:DeleteMessageBatch
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - sqs:ChangeMessageVisibility
                Resource:
                  - !GetAtt ProductPipelineExtractAttributeQ.Arn
                  - !GetAtt ProductPipelineCompleteQ.Arn
        - PolicyName: CloudWatchLogsCreate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
  ProductPipelineExtractAttributeInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: !Sub "product-pipeline-extract-attribute-profile-dev"
      Roles:
        - !Ref ProductPipelineExtractAttributeRole
  ProductPipelineExtractAttributeLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: !Sub "product-pipeline-extract-attribute-template-dev"
      LaunchTemplateData:
        KeyName: !Ref Ec2KeyPairName
        IamInstanceProfile:
          Name: !Ref ProductPipelineExtractAttributeInstanceProfile
        InstanceType: g6.xlarge # Placeholder - ASG overrides this
        ImageId: "{{resolve:ssm:/amis/dev/product-pipeline-extract-attribute/latest}}"
        SecurityGroupIds: [!Ref ProductPipelineSecurityGroup]
        BlockDeviceMappings:
          - DeviceName: /dev/sda1
            Ebs:
              VolumeSize: 80
              VolumeType: gp3
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: !Sub "product-pipeline-extract-attribute-dev"
              - Key: Fleet
                Value: product-pipeline-extract-attribute
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail
            sudo -i
            export STAGE="dev"
            export STAGE_NAME=dev
            export CommitSHA="${CommitSHA}"
            export ARTIFACT_BUCKET="${ArtifactBucket}"
            export SOURCE_QUEUE_URL="${ProductPipelineExtractAttributeQ}"
            export TARGET_QUEUE_URL="${ProductPipelineCompleteQ}"
            # Download and install binary
            sudo aws s3 cp "s3://${ArtifactBucket}/dev/${CommitSHA}/product-pipeline-extract-attribute" /opt/product-pipeline-extract-attribute/product-pipeline-extract-attribute --region eu-central-1
            sudo chmod +x /opt/product-pipeline-extract-attribute/product-pipeline-extract-attribute
            # Prepare (preconfigured via custom ami) python venv
            cd /opt/product-pipeline-extract-attribute
            source .venv/bin/activate
            # Run binary
            LOG_PATH="/var/log/product-pipeline-extract-attribute.log"
            echo "[$(date)] Running product-pipeline-extract-attribute..." | tee -a "$LOG_PATH"
            ./product-pipeline-extract-attribute >> "$LOG_PATH" 2>&1
            EXIT_CODE=$?
            echo "[$(date)] Job finished with code $EXIT_CODE" | tee -a "$LOG_PATH"
            sleep 5
            aws autoscaling terminate-instance-in-auto-scaling-group \
              --instance-id $(ec2metadata --instance-id) \
              --should-decrement-desired-capacity
            exit $EXIT_CODE
  ProductPipelineExtractAttributeASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      VPCZoneIdentifier:
        - !Ref ProductPipelineSubnetA
      MinSize: 0
      MaxSize: 1
      DesiredCapacity: 0
      MixedInstancesPolicy:
        LaunchTemplate:
          LaunchTemplateSpecification:
            LaunchTemplateId: !Ref ProductPipelineExtractAttributeLaunchTemplate
            Version: !GetAtt ProductPipelineExtractAttributeLaunchTemplate.LatestVersionNumber
          Overrides:
            - InstanceType: g6.xlarge
            - InstanceType: g6.2xlarge
            - InstanceType: g5.xlarge
            - InstanceType: g5.2xlarge
        InstancesDistribution:
          OnDemandPercentageAboveBaseCapacity: 0
          SpotAllocationStrategy: price-capacity-optimized
      Tags:
        - Key: Name
          Value: !Sub "product-pipeline-extract-attribute-dev"
          PropagateAtLaunch: true
      HealthCheckType: EC2
      HealthCheckGracePeriod: 120

  ProductPipelineCompleteDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-complete-dlq-dev"
      MessageRetentionPeriod: 1209600
  ProductPipelineCompleteQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: "product-pipeline-complete-queue-dev"
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ProductPipelineCompleteDlq.Arn
        maxReceiveCount: 5
      VisibilityTimeout: 60
  ProductPipelineCompleteRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-complete-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
          - Effect: Allow
            Principal:
              Service: spot.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: AllowTerminateInASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: autoscaling:TerminateInstanceInAutoScalingGroup
                Resource: "*"
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:Query
                  - dynamodb:PutItem
                  - dynamodb:DeleteItem
                  - dynamodb:UpdateItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:TransactGetItems
                  - dynamodb:TransactWriteItems
                Resource:
                  - !GetAtt TableOne.Arn
                  - !Sub "${TableOne.Arn}/index/*"
        - PolicyName: SQSPollerAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessage
                  - sqs:SendMessageBatch
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:DeleteMessageBatch
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - sqs:ChangeMessageVisibility
                Resource: !GetAtt ProductPipelineCompleteQ.Arn
        - PolicyName: CloudWatchLogsCreate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
        - PolicyName: SSMParameterAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - ssm:GetParameter
                  - ssm:GetParameters
                Resource:
                  - !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/opensearch/dev/username"
                  - !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/opensearch/dev/password"
  ProductPipelineCompleteInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: "product-pipeline-complete-profile-dev"
      Roles:
        - !Ref ProductPipelineCompleteRole
  ProductPipelineCompleteLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: "product-pipeline-complete-template-dev"
      LaunchTemplateData:
        KeyName: !Ref Ec2KeyPairName
        IamInstanceProfile:
          Name: !Ref ProductPipelineCompleteInstanceProfile
        InstanceType: c7i.large
        ImageId: ami-004e960cde33f9146
        SecurityGroupIds: [!Ref ProductPipelineSecurityGroup]
        BlockDeviceMappings:
          - DeviceName: /dev/sda1
            Ebs:
              VolumeSize: 50
              VolumeType: gp3
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: "product-pipeline-complete-dev"
              - Key: Fleet
                Value: product-pipeline-complete
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail
            sudo -i
            export STAGE="dev"
            export STAGE_NAME=dev
            export CommitSHA="${CommitSHA}"
            export ARTIFACT_BUCKET="${ArtifactBucket}"
            export SOURCE_QUEUE_URL="${ProductPipelineCompleteQ}"
            export DYNAMODB_TABLE_NAME="${TableOne}"
            export OPENSEARCH_ENDPOINT_URL="${OpenSearchEndpointUrl}"
            # Install awscli
            apt -y install unzip
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
            # Fetch OpenSearch credentials from SSM Parameter Store
            export OPENSEARCH_USERNAME=$(aws ssm get-parameter --name /opensearch/dev/username --query 'Parameter.Value' --output text --region eu-central-1)
            export OPENSEARCH_PASSWORD=$(aws ssm get-parameter --name /opensearch/dev/password --query 'Parameter.Value' --output text --region eu-central-1)
            # Install cloudwatch-agent
            sudo wget https://s3.amazonaws.com/amazoncloudwatch-agent/debian/amd64/latest/amazon-cloudwatch-agent.deb
            sudo dpkg -i -E ./amazon-cloudwatch-agent.deb
            # Download and install binary
            sudo aws s3 cp "s3://${ArtifactBucket}/dev/${CommitSHA}/product-pipeline-complete" /opt/product-pipeline-complete --region eu-central-1
            sudo chmod +x /opt/product-pipeline-complete
            # Configure CloudWatchAgent
            cat << EOF | sudo tee /opt/aws/amazon-cloudwatch-agent.json > /dev/null
            {
              "metrics": {
                "namespace": "CWAgent",
                "metrics_collected": {
                  "cpu": {
                    "measurement": [
                      {
                        "name": "cpu_usage_idle",
                        "rename": "CPU_IDLE",
                        "unit": "Percent"
                      },
                      {
                        "name": "cpu_usage_iowait",
                        "rename": "CPU_IOWAIT",
                        "unit": "Percent"
                      },
                      "cpu_time_guest"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"],
                    "totalcpu": false
                  },
                  "disk": {
                    "measurement": [
                      {
                        "name": "used_percent",
                        "rename": "DISK_USED_PERCENT",
                        "unit": "Percent"
                      },
                      "used",
                      "free"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  },
                  "diskio": {
                    "measurement": [
                      "io_time",
                      "read_bytes",
                      "write_bytes",
                      "reads",
                      "writes"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  },
                  "mem": {
                    "measurement": [
                      {
                        "name": "mem_used_percent",
                        "rename": "MEM_USED_PERCENT",
                        "unit": "Percent"
                      },
                      "mem_used",
                      "mem_available"
                    ],
                    "metrics_collection_interval": 60
                  },
                  "netstat": {
                    "measurement": [
                      "tcp_established",
                      "tcp_time_wait"
                    ],
                    "metrics_collection_interval": 60
                  },
                  "net": {
                    "measurement": [
                      "bytes_sent",
                      "bytes_recv",
                      "packets_sent",
                      "packets_recv"
                    ],
                    "metrics_collection_interval": 60,
                    "resources": ["*"]
                  }
                }
              },
              "logs": {
                "logs_collected": {
                  "files": {
                    "collect_list": [
                      {
                        "file_path": "/var/log/product-pipeline-complete.log",
                        "log_group_name": "/aws/ec2/$STAGE-product-pipeline-complete",
                        "log_stream_name": "{instance_id}",
                        "timestamp_format": "%Y-%m-%dT%H:%M:%S.%f%z",
                        "timezone": "UTC",
                        "multi_line_start_pattern": "^{"
                      }
                    ]
                  }
                }
              }
            }
            EOF
            amazon-cloudwatch-agent-ctl -a fetch-config -m ec2 -c file:/opt/aws/amazon-cloudwatch-agent.json -s
            LOG_PATH="/var/log/product-pipeline-complete.log"
            sudo touch "$LOG_PATH"
            sudo chmod 666 "$LOG_PATH"
            # Run binary
            echo "[$(date)] Running product-pipeline-complete..." | tee -a "$LOG_PATH"
            /opt/product-pipeline-complete >> "$LOG_PATH" 2>&1
            EXIT_CODE=$?
            echo "[$(date)] Job finished with code $EXIT_CODE" | tee -a "$LOG_PATH"
            sleep 5
            aws autoscaling terminate-instance-in-auto-scaling-group \
              --instance-id $(ec2metadata --instance-id) \
              --should-decrement-desired-capacity
            exit $EXIT_CODE
  ProductPipelineCompleteASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      VPCZoneIdentifier:
        - !Ref ProductPipelineSubnetA
      MinSize: 0
      MaxSize: 1
      DesiredCapacity: 0
      MixedInstancesPolicy:
        LaunchTemplate:
          LaunchTemplateSpecification:
            LaunchTemplateId: !Ref ProductPipelineCompleteLaunchTemplate
            Version: !GetAtt ProductPipelineCompleteLaunchTemplate.LatestVersionNumber
          Overrides:
            - InstanceType: c7i.large
            - InstanceType: c6i.large
        InstancesDistribution:
          OnDemandPercentageAboveBaseCapacity: 0
          SpotAllocationStrategy: price-capacity-optimized
      Tags:
        - Key: Name
          Value: "product-pipeline-complete-dev"
          PropagateAtLaunch: true
      HealthCheckType: EC2
      HealthCheckGracePeriod: 120

  ProductPipelineScaleControlRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-asg-scale-control-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SqsGetQueueAttributes
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:GetQueueAttributes
                  - sqs:GetQueueUrl
                  - cloudwatch:GetMetricStatistics
                Resource: "*"
        - PolicyName: ScaleASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - autoscaling:UpdateAutoScalingGroup
                  - autoscaling:DescribeAutoScalingGroups
                Resource: "*"
  ProductPipelineScaleControlLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-pipeline-asg-scale-control-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductPipelineScaleControlRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-pipeline-asg-scale-control-dev-${CommitSHA}.zip"
      MemorySize: 128
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          PRODUCT_PIPELINE_INIT_SQS_URL: !Ref ProductPipelineInitQ
          PRODUCT_PIPELINE_INIT_SQS_NAME: !GetAtt ProductPipelineInitQ.QueueName
          PRODUCT_PIPELINE_INIT_ASG_NAME: !Ref ProductPipelineInitASG
          PRODUCT_PIPELINE_TRANSLATE_SQS_URL: !Ref ProductPipelineTranslateQ
          PRODUCT_PIPELINE_TRANSLATE_SQS_NAME: !GetAtt ProductPipelineTranslateQ.QueueName
          PRODUCT_PIPELINE_TRANSLATE_ASG_NAME: !Ref ProductPipelineTranslateASG
          PRODUCT_PIPELINE_EMBED_TEXT_SQS_URL: !Ref ProductPipelineEmbedTextQ
          PRODUCT_PIPELINE_EMBED_TEXT_SQS_NAME: !GetAtt ProductPipelineEmbedTextQ.QueueName
          PRODUCT_PIPELINE_EMBED_TEXT_ASG_NAME: !Ref ProductPipelineEmbedTextASG
  ProductPipelineScaleControlStartSchedule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: "cron(*/15 * * * ? *)" # every 15min
      Targets:
        - Arn: !GetAtt ProductPipelineScaleControlLambda.Arn
          Id: "ProductPipelineScaleControlStartSchedule"
          RetryPolicy:
            MaximumRetryAttempts: 5
            MaximumEventAgeInSeconds: 3600
          DeadLetterConfig:
            Arn: !GetAtt ProductPipelineInitDlq.Arn
  ProductPipelineScaleControlStartSchedulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ProductPipelineScaleControlLambda
      Action: "lambda:InvokeFunction"
      Principal: "events.amazonaws.com"
      SourceArn: !GetAtt ProductPipelineScaleControlStartSchedule.Arn

  ProductPipelineScalePersistUpRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "product-pipeline-asg-scale-persist-up-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: ScalePersistUpASG
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - autoscaling:UpdateAutoScalingGroup
                  - autoscaling:DescribeAutoScalingGroups
                Resource: !Sub "arn:aws:autoscaling:${AWS::Region}:${AWS::AccountId}:autoScalingGroup:*:autoScalingGroupName/${ProductPipelineCompleteASG}"
  ProductPipelineScalePersistUpLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: "product-pipeline-asg-scale-persist-up-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt ProductPipelineScalePersistUpRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "product-pipeline-asg-scale-persist-up-dev-${CommitSHA}.zip"
      MemorySize: 128
      Timeout: 60
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          PRODUCT_PIPELINE_COMPLETE_ASG_NAME: !Ref ProductPipelineCompleteASG
  ProductPipelineScalePersistUpStartSchedule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: "cron(0 1 * * ? *)" # every day at 01:00 UTC
      Targets:
        - Arn: !GetAtt ProductPipelineScalePersistUpLambda.Arn
          Id: "ProductPipelineScalePersistUpStartSchedule"
          RetryPolicy:
            MaximumRetryAttempts: 5
            MaximumEventAgeInSeconds: 3600
          DeadLetterConfig:
            Arn: !GetAtt ProductPipelineCompleteDlq.Arn
  ProductPipelineScalePersistUpStartSchedulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ProductPipelineScalePersistUpLambda
      Action: "lambda:InvokeFunction"
      Principal: "events.amazonaws.com"
      SourceArn: !GetAtt ProductPipelineScalePersistUpStartSchedule.Arn

  FxRateSyncRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "fxrate-lambda-role-dev"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                Resource:
                  - !GetAtt TableOne.Arn
  FxRateSyncLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub "fxrate-lambda-dev"
      Runtime: provided.al2023
      Handler: lib.handler
      Role: !GetAtt FxRateSyncRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Sub "fxrate-lambda-dev-${CommitSHA}.zip"
      MemorySize: 128
      Timeout: 180
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref TableOne
          FXRATES_API_TOKEN: "{{resolve:ssm:/fxratesapi/prod/api-token}}"
  FxRateSyncStartSchedule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: "cron(0 6,18 * * ? *)" # 6 AM/PM
      Targets:
        - Arn: !GetAtt FxRateSyncLambda.Arn
          Id: "FxRateSyncStartSchedule"
          RetryPolicy:
            MaximumRetryAttempts: 3
            MaximumEventAgeInSeconds: 3600
  FxRateSyncStartSchedulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref FxRateSyncLambda
      Action: "lambda:InvokeFunction"
      Principal: "events.amazonaws.com"
      SourceArn: !GetAtt FxRateSyncStartSchedule.Arn

Outputs:
  CognitoHostedUIDomain:
    Value: !Sub "https://${PrimaryUserPoolDomain}.auth.${AWS::Region}.amazoncognito.com"
  CognitoUserPoolId:
    Value: !Ref PrimaryUserPool
  CognitoUserPoolClientPublicId:
    Value: !Ref PrimaryUserPoolClientPublic
  CognitoUserPoolClientAdminId:
    Value: !Ref PrimaryUserPoolClientAdmin
  ApiGatewayEndpointUrl:
    Value: !Sub "https://${Api}.execute-api.${AWS::Region}.amazonaws.com/dev"
  OpensearchEndpointUrl:
    Value: !Ref OpenSearchEndpointUrl
  OpensearchDomainName:
    Value: "aura-historia-dev"
  DynamodbTable1Name:
    Value: !Ref TableOne

  SendMailQueueUrl:
    Value: !Ref SendMailQ
  SendMailDeadLetterQueueUrl:
    Value: !Ref SendMailDlq

  ProductMaterializeDynamodbNewQueueUrl:
    Value: !Ref ProductMaterializeDynamoDbNewQ
  ProductMaterializeDynamodbNewDeadLetterQueueUrl:
    Value: !Ref ProductMaterializeDynamoDbNewDlq

  ProductMaterializeDynamodbUpdateQueueUrl:
    Value: !Ref ProductMaterializeDynamoDbUpdateQ
  ProductMaterializeDynamodbUpdateDeadLetterQueueUrl:
    Value: !Ref ProductMaterializeDynamoDbUpdateDlq

  ProductMaterializeOpensearchNewQueueUrl:
    Value: !Ref ProductMaterializeOpenSearchNewQ
  ProductMaterializeOpensearchNewDeadLetterQueueUrl:
    Value: !Ref ProductMaterializeOpenSearchNewDlq

  ProductMaterializeOpensearchUpdateQueueUrl:
    Value: !Ref ProductMaterializeOpenSearchUpdateQ
  ProductMaterializeOpensearchUpdateDeadLetterQueueUrl:
    Value: !Ref ProductMaterializeOpenSearchUpdateDlq

  ProductUpdateNotifyUserQueueUrl:
    Value: !Ref ProductUpdateNotifyUserQ
  ProductUpdateNotifyUserDeadLetterQueueUrl:
    Value: !Ref ProductUpdateNotifyUserDlq

  ProductPipelineInitQueueUrl:
    Value: !Ref ProductPipelineInitQ
  ProductPipelineInitDeadLetterQueueUrl:
    Value: !Ref ProductPipelineInitDlq

  ProductPipelineTranslateQueueUrl:
    Value: !Ref ProductPipelineTranslateQ
  ProductPipelineTranslateDeadLetterQueueUrl:
    Value: !Ref ProductPipelineTranslateDlq

  ProductPipelineEmbedTextQueueUrl:
    Value: !Ref ProductPipelineEmbedTextQ
  ProductPipelineEmbedTextDeadLetterQueueUrl:
    Value: !Ref ProductPipelineEmbedTextDlq

  ProductPipelineCompleteQueueUrl:
    Value: !Ref ProductPipelineCompleteQ
  ProductPipelineCompleteDeadLetterQueueUrl:
    Value: !Ref ProductPipelineCompleteDlq

  ShopOpensearchIndexQueueUrl:
    Value: !Ref ShopOpenSearchIndexQ
  ShopOpensearchIndexDeadLetterQueueUrl:
    Value: !Ref ShopOpenSearchIndexDlq
