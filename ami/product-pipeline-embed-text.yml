name: setup-env-product-pipeline-embed-text
schemaVersion: 1.0
parameter:
  - Stage:
      type: string
phases:
  - name: build
    steps:
      - name: InstallApp
        action: ExecuteBash
        inputs:
          commands:
            - echo "Installing foo"
            - mkdir -p /opt/foo
