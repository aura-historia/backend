[package]
name = "watchlist-core"
version = "0.1.0"
edition = "2024"

[dependencies]
product-listing-core = { workspace = true }
strum = { workspace = true }
strum_macros = { workspace = true }
user-core = { workspace = true }
fake = { workspace = true, optional = true }

[features]
default = []
test-data = ["fake", "product-listing-core/test-data", "user-core/test-data"]
