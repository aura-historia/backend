[package]
name = "watchlist-core"
version = "0.1.0"
edition = "2024"

[dependencies]
product-core = { workspace = true }
user-core = { workspace = true }
fake = { workspace = true, optional = true }

[features]
default = []
test-data = ["fake", "product-core/test-data", "user-core/test-data"]
