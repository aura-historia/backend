[package]
name = "shop-core"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { workspace = true }
time = { workspace = true }
url = { workspace = true }

# Optional
fake = { workspace = true, optional = true }

[dev-dependencies]
shop-core = { workspace = true, features= ["test-data"] }

[features]
default = []
test-data = ["fake", "common/test-data"]
